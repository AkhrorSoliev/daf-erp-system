# A1 — eshitish mashqi (`HOEREN_WAHL`) — amalga oshirish rejasi

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 1 va 2-unitning 12 dialogiga ovoz beriladi va har biriga 2 ta tushunish savoli qo'shiladi — o'quvchi suhbatni eshitib, savolga 3 variantdan javob beradi (`HOEREN_WAHL`).

**Architecture:** Savollar dialog faylining ichida (`fragen`), bazada yangi `DafHoerFrage` jadvali va `DafDialog.audioKey`. Dvigatel `hoer-fragen.ts` quruvchisi bilan yangi format va yangi `itemType: 'HOERFRAGE'` oladi; mijozda to'liq pleyer (0.8× tugmasi) va javobdan keyin ochiladigan transkript. Ovoz `text-to-dialogue/eleven-v3` bilan bitta faylga yasaladi, tasodifiy kalit bilan R2 ga yuklanadi, manifest `textHash` bilan eskirishdan himoyalanadi.

**Tech Stack:** NestJS, Prisma 7 (PostgreSQL/Neon), jest + ts-jest (server), vitest (client, komponent render YO'Q), Next.js + React + Tailwind, fal.ai (`fal-ai/elevenlabs/text-to-dialogue/eleven-v3`), Cloudflare R2.

## Global Constraints

- Dizayn: `docs/superpowers/specs/2026-09-11-a1-hoeren-design.md`. Ziddiyat chiqsa dizayn ustun.
- Ish `feat/a1-hoeren` shoxida, `.worktrees/a1-kichik-qarzlar` worktree'sida (shox allaqachon shu yerda). `git reset --hard` ishlatilmaydi.
- `server/node_modules` asosiy repo bilan umumiy (symlink): typecheck yolg'on xato bersa `npx prisma generate`.
- Har commit oldidan `npm test` va `npm run typecheck` (server), `npm test` va `npx tsc --noEmit` (client) o'tishi shart.
- **Pullik chaqiruv faqat Task 11 va 12 da**, va faqat CEO ruxsatidan keyin. Chaqiruvdan oldin narx `get_pricing` bilan qayta tekshiriladi (2026-09-11: $0.10 / 1 000 belgi).
- **To'g'ri javob mijozga savol bilan birga yuborilmaydi** (kurs dizayni D6/D7). Transkript faqat javobdan keyin.
- Audio kaliti **tasodifiy** (`neuerAudioSchluessel()`), so'zdan hisoblanmaydi.
- Ball yo'q, Leitner yo'q: `itemType !== 'WORT'` bo'lgani uchun mavjud kod buni o'zi beradi — hech qanday «ball bermasin» kodi yozilmaydi.
- Barcha o'zbekcha yozuv lotin alifbosida. Kod izohlari o'zbekcha (mavjud uslub).
- `prisma migrate dev` ISHLAMAYDI — `migrate diff` → SQL ni tozalash → `db execute` → `migrate resolve --applied`.
- `FrageFormat` ro'yxati uch joyda: server `frage.types.ts`, mijoz `lernen/types.ts`, mijoz `media/media-fragen-types.ts`. Uchalasi ham yangilanadi; `frage-format-parity.spec.ts` server bilan `media-fragen-types.ts` ni solishtiradi.

---

## File Structure

| Fayl | Vazifasi | Task |
| --- | --- | --- |
| `server/prisma/schema.prisma` | `DafDialog.audioKey`, `DafHoerFrage` modeli | 1 |
| `server/prisma/migrations/20260911120000_daf_hoer_frage/migration.sql` | Migratsiya | 1 |
| `server/src/daf/inhalt/unit-inhalt.types.ts` | `HoerFrage`, `Dialog.fragen` | 2 |
| `server/src/daf/inhalt/hoer-fragen.validate.ts` | Savol qoidalari (sof) | 2 |
| `server/src/daf/inhalt/hoer-fragen.validate.spec.ts` | Qoida testlari | 2 |
| `server/src/daf/inhalt/dialog-audio.ts` | `textHash`, manifest tipi (sof) | 2 |
| `server/src/daf/inhalt/dialog-audio.spec.ts` | Xesh testlari | 2 |
| `server/src/daf/inhalt/unit-inhalt.file.spec.ts` | Haqiqiy fayllarni qo'riqlaydi — savol va xesh qoidalari | 2 |
| `server/scripts/daf-inhalt-check.ts` | CLI qo'riqchi — savol qoidalari | 2 |
| `server/src/daf/inhalt/inhalt-seed.service.ts` | `audioKey`, `DafHoerFrage` seed, stale o'chirish tartibi | 3 |
| `server/src/daf/inhalt/inhalt-seed.service.spec.ts` | Seed testlari | 3 |
| `server/scripts/daf-inhalt-seed.ts` | `dialog-audio.json` ni o'qib beradi | 3 |
| `server/content/daf/a1/hilfswoerter.json` | 5 ta dialog ismi | 4 |
| `server/content/daf/a1/u01/dialoge.json`, `u02/dialoge.json` | 24 savol | 4 |
| `server/src/daf/uebung/frage.types.ts` | `ItemType`, `HOEREN_WAHL`, `MaterialDialog.audioKey/fragen`, `MaterialHoerFrage` | 5 |
| `server/src/daf/dto/uebung.dto.ts` | `ALLE_ITEM_TYPEN`, `ITEM_TYPEN` | 5 |
| `server/src/daf/dto/uebung.dto.spec.ts` | `ITEM_TYPEN` testi | 5 |
| `server/src/daf/uebung/dialog-fragen.ts` | `DIALOG_LUECKE` hamma satrni band qiladi | 6 |
| `server/src/daf/uebung/dialog-fragen.spec.ts` | Shu qoida testi | 6 |
| `server/src/daf/uebung/hoer-fragen.ts` | `hoerenWahl()` quruvchisi | 6 |
| `server/src/daf/uebung/hoer-fragen.spec.ts` | Quruvchi testlari | 6 |
| `server/src/daf/uebung/seans.ts` | Format bo'yicha chegara | 7 |
| `server/src/daf/uebung/seans.spec.ts` | Chegara testi | 7 |
| `server/src/daf/uebung/kind-formate.ts` | `UNIT_TEST` moyilligi | 7 |
| `server/src/daf/uebung/kind-formate.spec.ts` | Moyillik testi | 7 |
| `server/src/daf/uebung/uebung.service.ts` | Material yuklash, nomzod, `pruefen`, `transkript`, `ersatz` | 8 |
| `server/src/daf/uebung/uebung.service.spec.ts` | Dvigatel testlari | 8 |
| `server/src/daf/media/daf-media-fragen.service.ts` | `VORSCHAU_BAUER.HOEREN_WAHL`, material `fragen`/`audioKey` | 9 |
| `server/src/daf/media/daf-media-inhalt.service.ts` | `dialoge` ro'yxati (audio + savollar) | 9 |
| `server/src/daf/media/daf-media-inhalt.service.spec.ts` | Testi | 9 |
| `client/src/components/student-portal/lernen/types.ts` | `HOEREN_WAHL`, `HOERFRAGE`, `transkript` | 10 |
| `client/src/components/student-portal/lernen/uebung/koersatma.ts` (+test) | Ko'rsatma va `harakat` | 10 |
| `client/src/components/student-portal/lernen/uebung/pleyer-vaqt.ts` (+test) | `mm:ss` formatlash (sof) | 10 |
| `client/src/components/student-portal/lernen/uebung/suhbat-pleyer.tsx` | To'liq pleyer, 0.8× | 10 |
| `client/src/components/student-portal/lernen/uebung/transkript-blok.tsx` | Javobdan keyingi matn | 10 |
| `client/src/components/student-portal/lernen/uebung/seans-ekrani.tsx` | Format ulanishi | 10 |
| `client/src/components/media/media-fragen-types.ts`, `media-fragen-utils.ts`, `media-fragen-panel.tsx` | Format ro'yxati, shakl, nom | 10 |
| `client/src/components/media/media-inhalt-types.ts`, `media-inhalt-panel.tsx` | Dialog karnayi va savollari | 10 |
| `server/content/daf/a1/stimmen.json` | Ism → ElevenLabs ovozi | 11 |
| `server/src/daf/media/fal-client.ts` (+spec) | `dialog()` metodi | 11 |
| `server/scripts/daf-gen-dialog-audio.ts` (+spec) | Ovoz yasash skripti | 11 |
| `server/content/daf/a1/dialog-audio.json` | Manifest | 11, 12 |
| `server/package.json` | `daf:gen-dialog-audio` | 11 |

---

### Task 1: Ma'lumot modeli va migratsiya

**Files:**
- Modify: `server/prisma/schema.prisma` (`model DafDialog` ~3100, `model DafDialogLine` ~3122)
- Create: `server/prisma/migrations/20260911120000_daf_hoer_frage/migration.sql`

**Interfaces:**
- Produces: `DafDialog.audioKey: string | null`, `DafDialog.fragen: DafHoerFrage[]`; model `DafHoerFrage { id, code, dialogId, order, frageDe, frageUz, richtig, falsch: string[] }` with `@@unique([dialogId, order])`.

- [ ] **Step 1: Sxemani o'zgartiring**

`server/prisma/schema.prisma` da `model DafDialog` ga `audioKey` va `fragen` qo'shing, `DafDialogLine` dan keyin yangi model yozing:

```prisma
model DafDialog {
  id        Int      @id @default(autoincrement())
  /// Barqaror kalit: `u01-d1`.
  code      String   @unique
  unitId    Int
  sectionId Int
  titelDe   String
  titelUz   String
  /// Butun suhbatning R2 kaliti — `text-to-dialogue` bitta fayl beradi,
  /// satrma-satr emas. `null` — ovoz hali yasalmagan, eshitish savoli
  /// qurilmaydi. Manba — `content/daf/a1/dialog-audio.json`.
  audioKey  String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  unit    DafUnit         @relation(fields: [unitId], references: [id])
  section DafSection      @relation(fields: [sectionId], references: [id])
  zeilen  DafDialogLine[]
  fragen  DafHoerFrage[]

  @@index([sectionId])
}
```

`DafDialogLine` dan keyin:

```prisma
/// Suhbatni eshitgandan keyingi tushunish savoli (`HOEREN_WAHL`).
///
/// O'z `id` si bor, chunki javob tekshiruvi materialni `itemType`+`itemId`
/// bo'yicha o'qiydi (D7). `falsch` — ikki chalg'ituvchi; to'g'ri javob
/// `richtig`. Ballga tegmaydi, Leitner kuzatmaydi.
model DafHoerFrage {
  id       Int      @id @default(autoincrement())
  /// Barqaror kalit: `u02-d2-f1` (dialog kaliti + tartib).
  code     String   @unique
  dialogId Int
  order    Int
  frageDe  String
  frageUz  String
  richtig  String
  falsch   String[]

  dialog DafDialog @relation(fields: [dialogId], references: [id])

  @@unique([dialogId, order])
  @@index([dialogId])
}
```

- [ ] **Step 2: Migratsiya faylini yozing**

`server/prisma/migrations/20260911120000_daf_hoer_frage/migration.sql`:

```sql
-- AlterTable
ALTER TABLE "DafDialog" ADD COLUMN     "audioKey" TEXT;

-- CreateTable
CREATE TABLE "DafHoerFrage" (
    "id" SERIAL NOT NULL,
    "code" TEXT NOT NULL,
    "dialogId" INTEGER NOT NULL,
    "order" INTEGER NOT NULL,
    "frageDe" TEXT NOT NULL,
    "frageUz" TEXT NOT NULL,
    "richtig" TEXT NOT NULL,
    "falsch" TEXT[],

    CONSTRAINT "DafHoerFrage_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE UNIQUE INDEX "DafHoerFrage_code_key" ON "DafHoerFrage"("code");

-- CreateIndex
CREATE INDEX "DafHoerFrage_dialogId_idx" ON "DafHoerFrage"("dialogId");

-- CreateIndex
CREATE UNIQUE INDEX "DafHoerFrage_dialogId_order_key" ON "DafHoerFrage"("dialogId", "order");

-- AddForeignKey
ALTER TABLE "DafHoerFrage" ADD CONSTRAINT "DafHoerFrage_dialogId_fkey" FOREIGN KEY ("dialogId") REFERENCES "DafDialog"("id") ON DELETE RESTRICT ON UPDATE CASCADE;
```

- [ ] **Step 3: Diff bilan solishtiring**

Run (worktree `server/` dan; `DATABASE_URL` asosiy repo `.env` dan, `export DATABASE_URL="$(grep -m1 '^DATABASE_URL=' /Users/a1111/Desktop/daf-erp-system/server/.env | cut -d= -f2- | tr -d '"'"'"'\r')"`):
`npx prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --script`
Expected: chiqishda faqat `DafDialog.audioKey` va `DafHoerFrage` ga tegishli satrlar SIZNING faylingiz bilan bir xil; boshqa (eski drift) satrlar e'tiborga olinmaydi.

- [ ] **Step 4: Dev bazaga qo'llang va yozib qo'ying**

Run: `npx prisma db execute --file prisma/migrations/20260911120000_daf_hoer_frage/migration.sql`
Run: `npx prisma migrate resolve --applied 20260911120000_daf_hoer_frage`
Run: `npx prisma generate`
Expected: uchalasi xatosiz.

- [ ] **Step 5: Typecheck va test**

Run: `npm run typecheck && npm test --silent 2>&1 | tail -4`
Expected: typecheck toza, hamma test o'tadi.

- [ ] **Step 6: Commit**

```bash
git add prisma/schema.prisma prisma/migrations/20260911120000_daf_hoer_frage/migration.sql
git commit -m "DafHoerFrage jadvali va DafDialog.audioKey

Eshitish savoliga o'z id si kerak (D7 — javob tekshiruvi materialni
itemType+itemId bo'yicha o'qiydi); text-to-dialogue butun suhbatni
bitta fayl qilib beradi, shuning uchun kalit satrda emas, dialogda.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: Kontent tiplari, savol qoidalari, `textHash`

**Files:**
- Modify: `server/src/daf/inhalt/unit-inhalt.types.ts` (`interface Dialog`, ~99)
- Create: `server/src/daf/inhalt/hoer-fragen.validate.ts`
- Create: `server/src/daf/inhalt/hoer-fragen.validate.spec.ts`
- Create: `server/src/daf/inhalt/dialog-audio.ts`
- Create: `server/src/daf/inhalt/dialog-audio.spec.ts`
- Modify: `server/src/daf/inhalt/unit-inhalt.file.spec.ts` (`describe.each(UNITS)('%s — dialoglar'` bloki)
- Modify: `server/scripts/daf-inhalt-check.ts`

**Interfaces:**
- Produces:
  - `interface HoerFrage { frageDe: string; frageUz: string; richtig: string; falsch: string[] }`; `Dialog.fragen?: HoerFrage[]` (ixtiyoriy — u03+ hali yozilmagan bo'lishi mumkin; qo'riqchi yozilgan unitda MAJBURIY qiladi).
  - `validateHoerFragen(dialog: Dialog, unbekannt: (text: string) => string[]): string[]`
  - `dialogTextHash(zeilen: Array<{ sprecher: string; de: string; tts?: string }>): string` (sha256 hex, 16 belgi)
  - `type DialogAudioManifest = Record<string, { key: string; textHash: string }>`
  - `validateDialogAudio(dialoge: Dialog[], manifest: DialogAudioManifest): string[]`

- [ ] **Step 1: Tiplarni qo'shing**

`unit-inhalt.types.ts` da `Dialog` dan OLDIN:

```ts
/**
 * Suhbatni eshitgandan keyingi tushunish savoli (`HOEREN_WAHL`).
 *
 * Dialog faylining ICHIDA (dizayn Q4): savol o'z suhbatisiz ma'nosiz —
 * bittasi o'zgarsa ikkinchisi ko'z oldida. Barqaror kalit seedda
 * dialog kaliti + tartibdan yasaladi (`u02-d2-f1`).
 */
export interface HoerFrage {
  frageDe: string;
  frageUz: string;
  richtig: string;
  /** Ikki chalg'ituvchi — odam yozadi, har biri suhbatdan aniq xato. */
  falsch: string[];
}
```

`Dialog` ga maydon:

```ts
export interface Dialog {
  /** Barqaror kalit: `u01-d1`. */
  id: string;
  section: string;
  titelDe: string;
  titelUz: string;
  zeilen: DialogZeile[];
  /**
   * Ixtiyoriy TIPDA, MAJBURIY QO'RIQCHIDA: matni yozilgan unitda har
   * dialogda aniq 2 savol bo'lishi kerak (`unit-inhalt.file.spec.ts`).
   * Tip ixtiyoriy, chunki eski testlar va yarim yozilgan unit
   * `fragen`siz dialog beradi.
   */
  fragen?: HoerFrage[];
}
```

- [ ] **Step 2: Yiqiladigan validator testini yozing**

`server/src/daf/inhalt/hoer-fragen.validate.spec.ts`:

```ts
import { validateHoerFragen } from './hoer-fragen.validate';
import type { Dialog } from './unit-inhalt.types';

const basis = (): Dialog => ({
  id: 'u02-d2',
  section: 'u02-s1',
  titelDe: 'Meine Schwester',
  titelUz: 'Mening opam',
  zeilen: [
    { sprecher: 'Anna', de: 'Ist das deine Schwester?', uz: 'x' },
    { sprecher: 'Jonas', de: 'Nein, das ist meine Tochter.', uz: 'x' },
    { sprecher: 'Anna', de: 'Wo wohnen sie?', uz: 'x' },
    { sprecher: 'Jonas', de: 'Meine Eltern wohnen in Deutschland.', uz: 'x' },
  ],
  fragen: [
    {
      frageDe: 'Wer ist Nodira?',
      frageUz: 'Nodira kim?',
      richtig: 'die Tochter',
      falsch: ['die Schwester', 'die Mutter'],
    },
    {
      frageDe: 'Wo wohnen die Eltern?',
      frageUz: "Ota-onasi qayerda yashaydi?",
      richtig: 'in Deutschland',
      falsch: ['in Usbekistan', 'hier'],
    },
  ],
});

// Progressiya funksiyasi testda soxta: hech qanday so'z notanish emas.
const allesBekannt = (): string[] => [];

describe('validateHoerFragen', () => {
  it('toza dialogda muammo yo`q', () => {
    expect(validateHoerFragen(basis(), allesBekannt)).toEqual([]);
  });

  it('savol soni aniq 2 bo`lishi kerak', () => {
    const d = basis();
    d.fragen = [d.fragen![0]];
    expect(validateHoerFragen(d, allesBekannt)).toEqual([
      'u02-d2: 1 ta savol — aniq 2 kerak',
    ]);
  });

  it('fragen yo`q bo`lsa ham 2 kerak deb yiqiladi', () => {
    const d = basis();
    delete d.fragen;
    expect(validateHoerFragen(d, allesBekannt)).toEqual([
      'u02-d2: 0 ta savol — aniq 2 kerak',
    ]);
  });

  it('to`g`ri javob chalg`ituvchilar orasida takrorlansa yiqiladi', () => {
    const d = basis();
    d.fragen![0].falsch = ['die Tochter.', 'die Mutter'];
    expect(validateHoerFragen(d, allesBekannt)).toEqual([
      "u02-d2-f1: variantlar har xil emas — «die Tochter» takror",
    ]);
  });

  it('uchta variant bo`lmasa yiqiladi', () => {
    const d = basis();
    d.fragen![1].falsch = ['hier'];
    expect(validateHoerFragen(d, allesBekannt)).toEqual([
      'u02-d2-f2: 2 variant — 3 kerak (richtig + 2 falsch)',
    ]);
  });

  it('notanish so`z savol, javob va chalg`ituvchida ushlanadi', () => {
    const d = basis();
    const unbekannt = (text: string): string[] =>
      text.includes('Usbekistan') ? ['usbekistan'] : [];
    expect(validateHoerFragen(d, unbekannt)).toEqual([
      'u02-d2-f2: notanish so`z — usbekistan',
    ]);
  });

  it('frageUz bo`sh yoki kirillcha bo`lsa yiqiladi', () => {
    const d = basis();
    d.fragen![0].frageUz = 'Нодира ким?';
    d.fragen![1].frageUz = '   ';
    expect(validateHoerFragen(d, allesBekannt)).toEqual([
      'u02-d2-f1: frageUz lotin alifbosida emas',
      'u02-d2-f2: frageUz bo`sh',
    ]);
  });

  it('to`g`ri javobning mazmunli so`zi suhbatda bo`lmasa yiqiladi', () => {
    const d = basis();
    d.fragen![1].richtig = 'in Usbekistan';
    d.fragen![1].falsch = ['in Deutschland', 'hier'];
    expect(validateHoerFragen(d, allesBekannt)).toEqual([
      'u02-d2-f2: to`g`ri javob «in Usbekistan» suhbatda aytilmagan',
    ]);
  });

  it('faqat yordamchi so`zdan iborat javobga suhbat qoidasi qo`llanmaydi', () => {
    // «ja» — ma'noli so'zsiz; mashina bu yerda hech narsa deya olmaydi.
    const d = basis();
    d.fragen![0].richtig = 'ja';
    d.fragen![0].falsch = ['nein', 'nicht'];
    expect(validateHoerFragen(d, allesBekannt)).toEqual([]);
  });
});
```

- [ ] **Step 3: Testni yuritib yiqilishini tasdiqlang**

Run: `npx jest src/daf/inhalt/hoer-fragen.validate.spec.ts`
Expected: FAIL — `Cannot find module './hoer-fragen.validate'`.

- [ ] **Step 4: Validatorni yozing**

`server/src/daf/inhalt/hoer-fragen.validate.ts`:

```ts
import { normalisieren } from '../uebung/antwort';
import { tokensOf } from './progression';
import type { Dialog } from './unit-inhalt.types';

/** Har dialogda aniq shuncha savol — dizayn 4.2. */
export const FRAGEN_PRO_DIALOG = 2;

/**
 * «To'g'ri javob suhbatda aytilgan» tekshiruvida HISOBGA OLINMAYDIGAN
 * so'zlar: artikl, old ko'makchi, `sein` shakllari, olmosh, bog'lovchi.
 *
 * `hilfswoerter.json` EMAS — u yerda atoqli otlar ham bor, «Nodira»
 * esa aynan tekshirilishi kerak bo'lgan javob. Ro'yxat ataylab qisqa
 * va yopiq: kengaytirish qoidani bo'shatadi.
 */
const SCHWACHE_WOERTER = new Set([
  'der', 'die', 'das', 'ein', 'eine', 'den', 'dem',
  'in', 'aus', 'ist', 'sind', 'bin', 'bist',
  'er', 'sie', 'es', 'ich', 'du', 'wir', 'ihr',
  'und', 'oder', 'nicht', 'ja', 'nein', 'auch', 'sehr',
]);

const NICHT_LATEIN = /[Ѐ-ԯ؀-ۿ]/;

/**
 * Bitta dialogning eshitish savollari qoidalari (dizayn 4.2).
 *
 * `unbekannt` — progressiya funksiyasi: matndagi hali o'rgatilmagan
 * so'zlarni qaytaradi. Bu yerda EMAS, chaqiruvchida quriladi (bo'lim
 * tartibi va yordamchi so'zlar u yerda ma'lum) — validator sof qoladi.
 */
export function validateHoerFragen(
  dialog: Dialog,
  unbekannt: (text: string) => string[],
): string[] {
  const problems: string[] = [];
  const fragen = dialog.fragen ?? [];

  if (fragen.length !== FRAGEN_PRO_DIALOG) {
    problems.push(
      `${dialog.id}: ${fragen.length} ta savol — aniq ${FRAGEN_PRO_DIALOG} kerak`,
    );
  }

  // Suhbatda aytilgan so'zlar — `normalisieren` bilan, grading kabi.
  const gesagt = new Set(
    dialog.zeilen.flatMap((z) => tokensOf(normalisieren(z.de))),
  );

  fragen.forEach((f, i) => {
    const code = `${dialog.id}-f${i + 1}`;
    const varianten = [f.richtig, ...f.falsch];

    if (varianten.length !== 3) {
      problems.push(
        `${code}: ${varianten.length} variant — 3 kerak (richtig + 2 falsch)`,
      );
    }

    const gesehen = new Set<string>();
    for (const v of varianten) {
      const n = normalisieren(v);
      if (gesehen.has(n)) {
        problems.push(`${code}: variantlar har xil emas — «${v}» takror`);
      }
      gesehen.add(n);
    }

    const fremd = [
      ...unbekannt(f.frageDe),
      ...varianten.flatMap((v) => unbekannt(v)),
    ];
    if (fremd.length > 0) {
      problems.push(`${code}: notanish so\`z — ${[...new Set(fremd)].join(', ')}`);
    }

    if (f.frageUz.trim() === '') {
      problems.push(`${code}: frageUz bo\`sh`);
    } else if (NICHT_LATEIN.test(f.frageUz)) {
      problems.push(`${code}: frageUz lotin alifbosida emas`);
    }

    // Qo'pol tekshiruv: javobning kamida bitta MAZMUNLI so'zi suhbatda
    // aytilgan bo'lsin. Mazmunli so'z yo'q bo'lsa («ja») — odam ko'radi.
    const stark = tokensOf(normalisieren(f.richtig)).filter(
      (t) => !SCHWACHE_WOERTER.has(t),
    );
    if (stark.length > 0 && !stark.some((t) => gesagt.has(t))) {
      problems.push(
        `${code}: to\`g\`ri javob «${f.richtig}» suhbatda aytilmagan`,
      );
    }
  });

  return problems;
}
```

- [ ] **Step 5: Testni yuritib o'tishini tasdiqlang**

Run: `npx jest src/daf/inhalt/hoer-fragen.validate.spec.ts`
Expected: PASS (9 test).

- [ ] **Step 6: `textHash` testini yozing**

`server/src/daf/inhalt/dialog-audio.spec.ts`:

```ts
import { dialogTextHash, validateDialogAudio } from './dialog-audio';
import type { Dialog } from './unit-inhalt.types';

const dialog = (): Dialog => ({
  id: 'u02-d2',
  section: 'u02-s1',
  titelDe: 'x',
  titelUz: 'x',
  zeilen: [
    { sprecher: 'Anna', de: 'Ist das deine Schwester?', uz: 'x' },
    { sprecher: 'Jonas', de: 'Nein.', uz: 'x', tts: 'Nain.' },
  ],
});

describe('dialogTextHash', () => {
  it('bir xil matn — bir xil xesh, 16 belgi', () => {
    const a = dialogTextHash(dialog().zeilen);
    expect(a).toBe(dialogTextHash(dialog().zeilen));
    expect(a).toMatch(/^[0-9a-f]{16}$/);
  });

  it('tts o`zgarsa xesh o`zgaradi — ovoz aynan tts ni aytadi', () => {
    const z = dialog().zeilen;
    z[1] = { ...z[1], tts: 'Nein.' };
    expect(dialogTextHash(z)).not.toBe(dialogTextHash(dialog().zeilen));
  });

  it('gapiruvchi o`zgarsa xesh o`zgaradi — ovoz obrazga bog`liq', () => {
    const z = dialog().zeilen;
    z[0] = { ...z[0], sprecher: 'Mia' };
    expect(dialogTextHash(z)).not.toBe(dialogTextHash(dialog().zeilen));
  });

  it('uz o`zgarsa xesh o`zgarmaydi — tarjima aytilmaydi', () => {
    const z = dialog().zeilen;
    z[0] = { ...z[0], uz: 'boshqa' };
    expect(dialogTextHash(z)).toBe(dialogTextHash(dialog().zeilen));
  });
});

describe('validateDialogAudio', () => {
  it('manifestda yozuv yo`q — muammo emas (ovoz hali yasalmagan)', () => {
    expect(validateDialogAudio([dialog()], {})).toEqual([]);
  });

  it('xesh mos — toza', () => {
    const d = dialog();
    const manifest = {
      'u02-d2': { key: 'daf/audio/abc.mp3', textHash: dialogTextHash(d.zeilen) },
    };
    expect(validateDialogAudio([d], manifest)).toEqual([]);
  });

  it('matn o`zgargan — audio eski matnni aytyapti', () => {
    const d = dialog();
    const manifest = {
      'u02-d2': { key: 'daf/audio/abc.mp3', textHash: '0000000000000000' },
    };
    expect(validateDialogAudio([d], manifest)).toEqual([
      'u02-d2: dialog matni o`zgargan, audio eski matnni aytyapti — qayta yasang yoki manifestdan o`chiring',
    ]);
  });
});
```

- [ ] **Step 7: Testni yuritib yiqilishini tasdiqlang**

Run: `npx jest src/daf/inhalt/dialog-audio.spec.ts`
Expected: FAIL — `Cannot find module './dialog-audio'`.

- [ ] **Step 8: `dialog-audio.ts` ni yozing**

```ts
import { createHash } from 'crypto';
import type { Dialog, DialogZeile } from './unit-inhalt.types';

/**
 * Dialog audiosi manifesti — `content/daf/a1/dialog-audio.json`.
 *
 * `key` tasodifiy (so'z audiosidagi qoida, `audio-keys.ts`). `textHash`
 * — ovoz YASALGAN paytdagi matnning izi: matn keyin tahrirlansa
 * qo'riqchi yiqiladi, aks holda o'quvchi bir narsani eshitib, javobdan
 * keyin boshqa narsani o'qirdi.
 */
export type DialogAudioManifest = Record<
  string,
  { key: string; textHash: string }
>;

/**
 * Ovoz AYNAN nimani aytadi — shuning xeshi: gapiruvchi (obraz → ovoz)
 * va `tts ?? de` (aytiladigan matn). `uz` kirmaydi — u aytilmaydi.
 */
export function dialogTextHash(
  zeilen: Array<Pick<DialogZeile, 'sprecher' | 'de' | 'tts'>>,
): string {
  const matn = zeilen.map((z) => `${z.sprecher} ${z.tts ?? z.de}`).join('\n');
  return createHash('sha256').update(matn, 'utf8').digest('hex').slice(0, 16);
}

export function validateDialogAudio(
  dialoge: Dialog[],
  manifest: DialogAudioManifest,
): string[] {
  const problems: string[] = [];
  for (const d of dialoge) {
    const eintrag = manifest[d.id];
    if (!eintrag) continue;
    if (eintrag.textHash !== dialogTextHash(d.zeilen)) {
      problems.push(
        `${d.id}: dialog matni o\`zgargan, audio eski matnni aytyapti — qayta yasang yoki manifestdan o\`chiring`,
      );
    }
  }
  return problems;
}
```

- [ ] **Step 9: Testni yuritib o'tishini tasdiqlang**

Run: `npx jest src/daf/inhalt/dialog-audio.spec.ts`
Expected: PASS (7 test).

- [ ] **Step 10: Haqiqiy fayl qo'riqchisiga ikki test qo'shing**

`unit-inhalt.file.spec.ts` da importlar:

```ts
import { validateHoerFragen } from './hoer-fragen.validate';
import { validateDialogAudio, type DialogAudioManifest } from './dialog-audio';
```

`describe.each(UNITS)('%s — dialoglar'` blokining OXIRIGA (oxirgi `it` dan keyin, `});` dan oldin):

```ts
  it('har dialogda 2 ta eshitish savoli bor va ular qoidadan o`tadi', () => {
    // Savol, javob va chalg'ituvchining har so'zi shu bo'limgacha tanish —
    // gap va dialog bilan BIR XIL progressiya funksiyasi.
    const problems = dialoge.dialoge.flatMap((d) =>
      validateHoerFragen(d, (text) => unbekannteWoerter(d.section, text)),
    );
    expect(problems).toEqual([]);
  });

  it('audio manifesti dialog matni bilan mos', () => {
    // Ovoz yasalgandan keyin matn tahrirlansa, audio eski matnni
    // aytadi — bu qoida buni commit'dan OLDIN ushlaydi.
    const manifest = readOrEmpty<DialogAudioManifest>(
      {},
      'dialog-audio.json',
    );
    expect(validateDialogAudio(dialoge.dialoge, manifest)).toEqual([]);
  });
```

- [ ] **Step 11: Qo'riqchi yiqilishini ko'ring**

Run: `npx jest src/daf/inhalt/unit-inhalt.file.spec.ts 2>&1 | grep -E "●.*›|Tests:"`
Expected: `u01 — dialoglar › har dialogda 2 ta eshitish savoli bor` va `u02 — dialoglar › …` YIQILADI (savollar hali yozilmagan — Task 4 da yoziladi). Boshqa testlar o'tadi.

- [ ] **Step 12: CLI qo'riqchiga ham qo'shing**

`server/scripts/daf-inhalt-check.ts` da importlar:

```ts
import { validateHoerFragen } from '../src/daf/inhalt/hoer-fragen.validate';
import {
  validateDialogAudio,
  type DialogAudioManifest,
} from '../src/daf/inhalt/dialog-audio';
import {
  sectionsInCourseOrder,
  knownWordsBySection,
  hilfsSetFor,
  unknownWordsIn,
} from '../src/daf/inhalt/progression';
import type {
  WoerterFile,
  GrammatikFile,
  RedemittelFile,
  DialogeFile,
  HilfswoerterFile,
  Wort,
} from '../src/daf/inhalt/unit-inhalt.types';
```

`grammatik` blokidan KEYIN, `if (problems.length > 0)` dan OLDIN:

```ts
  const dialogePath = join(A1, code, 'dialoge.json');
  if (!existsSync(dialogePath)) {
    problems.push(`${code}: dialoge.json yo'q`);
  } else {
    const dialoge = read<DialogeFile>(code, 'dialoge.json');
    const kurs = read<KursFile>('kurs.json');
    const hilfswoerter = read<HilfswoerterFile>('hilfswoerter.json');
    const sections = sectionsInCourseOrder(kurs);
    // Tanish so'zlar — testdagi bilan bir xil manba: matni bor hamma unit.
    const alleWoerter: Wort[] = kurs.units
      .map((u) => u.code)
      .filter((c) => existsSync(join(A1, c, 'woerter.json')))
      .flatMap((c) => read<WoerterFile>(c, 'woerter.json').woerter);
    const known = knownWordsBySection(sections, alleWoerter);
    const unbekannt = (section: string) => (text: string) =>
      unknownWordsIn(
        text,
        known.get(section) ?? new Set<string>(),
        hilfsSetFor(section, hilfswoerter, sections),
      );
    problems.push(
      ...dialoge.dialoge.flatMap((d) =>
        validateHoerFragen(d, unbekannt(d.section)),
      ),
    );

    const manifestPath = join(A1, 'dialog-audio.json');
    const manifest: DialogAudioManifest = existsSync(manifestPath)
      ? read<DialogAudioManifest>('dialog-audio.json')
      : {};
    problems.push(...validateDialogAudio(dialoge.dialoge, manifest));
  }
```

- [ ] **Step 13: Typecheck va lint**

Run: `npm run typecheck && npx prettier --write scripts/daf-inhalt-check.ts src/daf/inhalt/*.ts && npx eslint src/daf/inhalt scripts/daf-inhalt-check.ts 2>&1 | tail -1`
Expected: typecheck toza, `0 errors`.

- [ ] **Step 14: Commit**

```bash
git add src/daf/inhalt/unit-inhalt.types.ts src/daf/inhalt/hoer-fragen.validate.ts src/daf/inhalt/hoer-fragen.validate.spec.ts src/daf/inhalt/dialog-audio.ts src/daf/inhalt/dialog-audio.spec.ts src/daf/inhalt/unit-inhalt.file.spec.ts scripts/daf-inhalt-check.ts
git commit -m "Eshitish savoli qoidalari va dialog audiosining textHash qo'riqchisi

Har dialogda aniq 2 savol, 3 har xil variant, progressiya, lotin
alifbosi, va qo'pol «javob suhbatda aytilgan» tekshiruvi. textHash —
ovoz yasalgandan keyin matn tahrirlansa qo'riqchi yiqiladi.

Qo'riqchi hozircha QIZIL: u01 va u02 savollari keyingi commitda.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: Seed — `audioKey`, savollar, stale o'chirish tartibi

**Files:**
- Modify: `server/src/daf/inhalt/inhalt-seed.service.ts` (`InhaltFiles`, `InhaltSeedReport`, dialog bloki ~210–270)
- Modify: `server/src/daf/inhalt/inhalt-seed.service.spec.ts`
- Modify: `server/scripts/daf-inhalt-seed.ts`

**Interfaces:**
- Consumes: `DialogAudioManifest`, `dialogTextHash` (Task 2), Prisma `dafHoerFrage` (Task 1).
- Produces: `InhaltFiles.dialogAudio?: DialogAudioManifest`; `InhaltSeedReport.hoerFragen: number`.

- [ ] **Step 1: Yiqiladigan testlarni yozing**

`inhalt-seed.service.spec.ts` da `files()` ning `dialoge` dialogiga savollar qo'shing:

```ts
          zeilen: [
            { sprecher: 'Anna', de: 'Hallo!', uz: 'Salom!' },
            { sprecher: 'Jonas', de: 'Hallo Anna!', uz: 'Salom Anna!' },
          ],
          fragen: [
            {
              frageDe: 'Wer ist das?',
              frageUz: 'Bu kim?',
              richtig: 'Anna',
              falsch: ['Mia', 'Jonas'],
            },
            {
              frageDe: 'Ist das Jonas?',
              frageUz: 'Bu Jonasmi?',
              richtig: 'ja',
              falsch: ['nein', 'nicht'],
            },
          ],
```

`fakePrisma()` ga jadval va model:

```ts
  const hoerFrage = fakeTable((a) => a.where.code, seqRef);
```

`rows` ga `hoerFrage: hoerFrage.map,`; qaytariladigan obyektga:

```ts
    dafHoerFrage: {
      upsert: hoerFrage.upsert,
      deleteMany: hoerFrage.deleteMany,
      count: hoerFrage.count,
    },
```

Mavjud «hamma turdagi materialni yozadi» testida kutilgan hisobotga `hoerFragen: 2,` qo'shing. Yangi testlar (`describe('InhaltSeedService'` ichiga):

```ts
  it('eshitish savollarini dialog kaliti + tartib bilan yozadi', async () => {
    const prisma = fakePrisma();
    await new InhaltSeedService(prisma as any).seed('u01', files());
    const codes = [...prisma.rows.hoerFrage.values()].map((r) => r.code);
    expect(codes).toEqual(['u01-d1-f1', 'u01-d1-f2']);
    const f1 = prisma.dafHoerFrage.upsert.mock.calls[0][0];
    expect(f1.create).toMatchObject({
      order: 1,
      frageDe: 'Wer ist das?',
      richtig: 'Anna',
      falsch: ['Mia', 'Jonas'],
    });
  });

  it('dialog audioKey ni manifestdan yozadi, yo`q bo`lsa null', async () => {
    const prisma = fakePrisma();
    const f = files();
    f.dialogAudio = {
      'u01-d1': { key: 'daf/audio/abc.mp3', textHash: 'irrelevant' },
    };
    await new InhaltSeedService(prisma as any).seed('u01', f);
    expect(prisma.dafDialog.upsert.mock.calls[0][0].create.audioKey).toBe(
      'daf/audio/abc.mp3',
    );
    expect(prisma.dafDialog.upsert.mock.calls[0][0].update.audioKey).toBe(
      'daf/audio/abc.mp3',
    );

    // Manifestdan olib tashlansa — baza ham null ga qaytadi (manifest manba).
    const yana = files();
    await new InhaltSeedService(prisma as any).seed('u01', yana);
    expect(prisma.dafDialog.upsert.mock.calls[1][0].update.audioKey).toBeNull();
  });

  it('fayldan yo`qolgan dialogning savollari satrlaridan OLDIN o`chiriladi', async () => {
    const prisma = fakePrisma();
    await new InhaltSeedService(prisma as any).seed('u01', files());
    const f = files();
    f.dialoge.dialoge[0].id = 'u01-d9';
    await new InhaltSeedService(prisma as any).seed('u01', f);
    // FK RESTRICT: savol → satr → dialog tartibida.
    const tartib = [
      prisma.dafHoerFrage.deleteMany.mock.invocationCallOrder[0],
      prisma.dafDialogLine.deleteMany.mock.invocationCallOrder[0],
      prisma.dafDialog.deleteMany.mock.invocationCallOrder[0],
    ];
    expect(tartib).toEqual([...tartib].sort((a, b) => a - b));
    expect([...prisma.rows.hoerFrage.values()].map((r) => r.code)).toEqual([
      'u01-d9-f1',
      'u01-d9-f2',
    ]);
  });

  it('ortiqcha savol tartibi o`chiriladi', async () => {
    const prisma = fakePrisma();
    await new InhaltSeedService(prisma as any).seed('u01', files());
    const call = prisma.dafHoerFrage.deleteMany.mock.calls.find(
      (c: any[]) => c[0].where.order?.gt !== undefined,
    );
    expect(call[0].where.order.gt).toBe(2);
  });
```

- [ ] **Step 2: Testni yuritib yiqilishini tasdiqlang**

Run: `npx jest src/daf/inhalt/inhalt-seed.service.spec.ts 2>&1 | grep -E "●.*›|Tests:"`
Expected: yangi 4 test va «hamma turdagi materialni yozadi» YIQILADI (`hoerFragen` yo'q, `dafHoerFrage` chaqirilmaydi).

- [ ] **Step 3: Servisni o'zgartiring**

`inhalt-seed.service.ts` importlari:

```ts
import type { DialogAudioManifest } from './dialog-audio';
```

`InhaltFiles` ga:

```ts
  /**
   * Dialog audiosi manifesti — `content/daf/a1/dialog-audio.json`.
   * Ixtiyoriy, `audio` bilan bir xil sabab. `DafDialog.audioKey` HAR
   * upsertda shundan yoziladi: manifest — yagona manba, baza — muhit;
   * yozuv yo'q bo'lsa `null` (eski kalit qolib ketmaydi).
   */
  dialogAudio?: DialogAudioManifest;
```

`InhaltSeedReport` ga `hoerFragen: number;`.

Stale dialog o'chirishda (`if (staleDialoge.length > 0)` bloki) — `dafDialogLine.deleteMany` dan OLDIN:

```ts
      // Savollar satrlardan OLDIN — `DafHoerFrage.dialogId` ham
      // `ON DELETE RESTRICT`. Bu qator yozilmasa, dialogni o'chirish
      // «faqat qo'shadi» degan migratsiyaga qaramay yiqiladi.
      await this.prisma.dafHoerFrage.deleteMany({
        where: { dialogId: { in: staleDialogIds } },
      });
```

Dialog upsert `data` obyektiga:

```ts
      const data = {
        unitId: unit.id,
        sectionId: sectionId.get(d.section) as number,
        titelDe: d.titelDe,
        titelUz: d.titelUz,
        audioKey: files.dialogAudio?.[d.id]?.key ?? null,
      };
```

Satrlar siklidan va ortiqcha satr o'chirishdan KEYIN, `regeln` dan OLDIN:

```ts
      // Eshitish savollari — `code` = dialog kaliti + tartib. Kalit
      // dialog ICHIDAGI tartibdan: o'rtadagi savol o'chirilsa qolgani
      // qayta raqamlanadi, ORTIQDA qolgani pastda o'chiriladi.
      for (const [i, f] of (d.fragen ?? []).entries()) {
        const order = i + 1;
        const fData = {
          frageDe: f.frageDe,
          frageUz: f.frageUz,
          richtig: f.richtig,
          falsch: f.falsch,
        };
        await this.prisma.dafHoerFrage.upsert({
          where: { code: `${d.id}-f${order}` },
          create: { code: `${d.id}-f${order}`, dialogId: row.id, order, ...fData },
          update: fData,
        });
        hoerFragen += 1;
      }
      await this.prisma.dafHoerFrage.deleteMany({
        where: { dialogId: row.id, order: { gt: (d.fragen ?? []).length } },
      });
```

`let dialoge = 0; let zeilen = 0;` yoniga `let hoerFragen = 0;`; qaytariladigan hisobotga `hoerFragen`.

- [ ] **Step 4: Skriptga manifestni o'qishni qo'shing**

`scripts/daf-inhalt-seed.ts` da `audio` o'qilgandan keyin:

```ts
  const dialogAudioPath = join(
    __dirname, '..', 'content', 'daf', 'a1', 'dialog-audio.json',
  );
  const dialogAudio: DialogAudioManifest = existsSync(dialogAudioPath)
    ? (JSON.parse(readFileSync(dialogAudioPath, 'utf8')) as DialogAudioManifest)
    : {};
```

importga `import type { DialogAudioManifest } from '../src/daf/inhalt/dialog-audio';`, `files` obyektiga `dialogAudio,`.

- [ ] **Step 5: Testlarni yuritib o'tishini tasdiqlang**

Run: `npx jest src/daf/inhalt/inhalt-seed.service.spec.ts`
Expected: PASS.

- [ ] **Step 6: Typecheck, lint, commit**

Run: `npm run typecheck && npx prettier --write src/daf/inhalt/inhalt-seed.service.ts src/daf/inhalt/inhalt-seed.service.spec.ts scripts/daf-inhalt-seed.ts && npx eslint src/daf/inhalt scripts/daf-inhalt-seed.ts 2>&1 | tail -1`

```bash
git add src/daf/inhalt/inhalt-seed.service.ts src/daf/inhalt/inhalt-seed.service.spec.ts scripts/daf-inhalt-seed.ts
git commit -m "Seed: dialog audioKey manifestdan, eshitish savollari, stale tartibi

audioKey har upsertda manifestdan yoziladi (yo'q bo'lsa null). Fayldan
yo'qolgan dialogning savollari satrlaridan OLDIN o'chiriladi — FK
RESTRICT, aks holda seed yiqiladi.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: 24 savol matni va 5 ism

**Files:**
- Modify: `server/content/daf/a1/hilfswoerter.json`
- Modify: `server/content/daf/a1/u01/dialoge.json`
- Modify: `server/content/daf/a1/u02/dialoge.json`

**Interfaces:**
- Consumes: `Dialog.fragen` (Task 2), qo'riqchi (Task 2).

- [ ] **Step 1: Dialog ismlarini yordamchi ro'yxatga qo'shing**

`hilfswoerter.json` `eintraege` massivining OXIRIGA (`weber` dan keyin):

```json
    { "wort": "walter", "grund": "atoqli ot — dialog obrazining ismi (u01-d2)" },
    { "wort": "doniyor", "grund": "atoqli ot — dialog obrazining ismi (u01-d4)" },
    { "wort": "sabine", "grund": "atoqli ot — dialog obrazining ismi (u01-d5)" },
    { "wort": "peter", "grund": "atoqli ot — dialog obrazining ismi (u01-d5)" },
    { "wort": "helga", "grund": "atoqli ot — dialog obrazining ismi (u01-d6)" }
```

- [ ] **Step 2: u01 savollarini yozing**

Har dialog obyektiga `zeilen` dan keyin `fragen`. Savoldagi har so'z o'sha bo'limgacha tanish bo'lishi shart (u01-s1 da faqat `wie` so'roq so'zi bor — `wer`/`was` s2, `wo`/`woher` s3).

`u01-d1`:
```json
   "fragen": [
    { "frageDe": "Ist das Mia, Anna oder Claudia?", "frageUz": "Bu Mia, Anna yoki Claudiami?", "richtig": "Mia", "falsch": ["Anna", "Claudia"] },
    { "frageDe": "Wie geht es Jonas?", "frageUz": "Jonasning ahvoli qanday?", "richtig": "gut", "falsch": ["nicht gut", "nicht sehr gut"] }
   ]
```
`u01-d2`:
```json
   "fragen": [
    { "frageDe": "Ist es Morgen oder Abend?", "frageUz": "Hozir ertalabmi yoki kech?", "richtig": "Abend", "falsch": ["Morgen", "Nacht"] },
    { "frageDe": "Wie geht es Mia?", "frageUz": "Mianing ahvoli qanday?", "richtig": "gut", "falsch": ["nicht gut", "nicht sehr gut"] }
   ]
```
`u01-d3`:
```json
   "fragen": [
    { "frageDe": "Wie heißt der Herr?", "frageUz": "Janobning ismi nima?", "richtig": "Markus", "falsch": ["Jonas", "Timur"] },
    { "frageDe": "Wie heißt die Frau?", "frageUz": "Ayolning ismi nima?", "richtig": "Claudia", "falsch": ["Anna", "Mia"] }
   ]
```
`u01-d4`:
```json
   "fragen": [
    { "frageDe": "Woher kommt Doniyor?", "frageUz": "Doniyor qayerdan?", "richtig": "aus Usbekistan", "falsch": ["aus Deutschland", "aus Usbekistan und aus Deutschland"] },
    { "frageDe": "Wo wohnt Doniyor?", "frageUz": "Doniyor qayerda yashaydi?", "richtig": "in Deutschland", "falsch": ["in Usbekistan", "nicht in Deutschland"] }
   ]
```
`u01-d5`:
```json
   "fragen": [
    { "frageDe": "Ist es vier oder fünf?", "frageUz": "To'rtmi yoki besh?", "richtig": "vier", "falsch": ["fünf", "neun"] },
    { "frageDe": "Null, sieben, zwei, neun, vier und …?", "frageUz": "Nol, yetti, ikki, to'qqiz, to'rt va …?", "richtig": "elf", "falsch": ["eins", "drei"] }
   ]
```
`u01-d6`:
```json
   "fragen": [
    { "frageDe": "Wie heißt die Frau?", "frageUz": "Ayolning ismi nima?", "richtig": "Anna Weber", "falsch": ["Anna Karimova", "Mia Weber"] },
    { "frageDe": "Ist das V oder W?", "frageUz": "Bu V mi yoki W?", "richtig": "W", "falsch": ["V", "Y"] }
   ]
```

- [ ] **Step 3: u02 savollarini yozing**

`u02-d1`:
```json
   "fragen": [
    { "frageDe": "Ist das der Vater oder der Bruder?", "frageUz": "Bu otasimi yoki akasi?", "richtig": "der Vater", "falsch": ["der Bruder", "der Sohn"] },
    { "frageDe": "Wer ist das? Die Mutter, die Schwester oder die Oma?", "frageUz": "Bu kim? Ona, opa yoki buvi?", "richtig": "die Mutter", "falsch": ["die Schwester", "die Oma"] }
   ]
```
`u02-d2`:
```json
   "fragen": [
    { "frageDe": "Wer ist Nodira?", "frageUz": "Nodira kim?", "richtig": "die Tochter", "falsch": ["die Schwester", "die Mutter"] },
    { "frageDe": "Wo wohnen die Eltern?", "frageUz": "Ota-onasi qayerda yashaydi?", "richtig": "in Deutschland", "falsch": ["in Usbekistan", "hier"] }
   ]
```
`u02-d3`:
```json
   "fragen": [
    { "frageDe": "Hat Timur ein Kind, zwei Kinder oder drei Kinder?", "frageUz": "Timurning bitta, ikkita yoki uchta bolasi bormi?", "richtig": "zwei Kinder", "falsch": ["ein Kind", "drei Kinder"] },
    { "frageDe": "Ist die Oma achtzig oder neunzig?", "frageUz": "Buvisi saksonmi yoki to'qson?", "richtig": "neunzig", "falsch": ["achtzig", "siebzig"] }
   ]
```
`u02-d4`:
```json
   "fragen": [
    { "frageDe": "Ist Nodira Ärztin oder Lehrerin?", "frageUz": "Nodira shifokormi yoki o'qituvchi?", "richtig": "Ärztin", "falsch": ["Lehrerin", "Arzt"] },
    { "frageDe": "Was ist Markus? Lehrer oder Arzt?", "frageUz": "Markus kim? O'qituvchimi yoki shifokor?", "richtig": "Lehrer", "falsch": ["Arzt", "Chef"] }
   ]
```
`u02-d5`:
```json
   "fragen": [
    { "frageDe": "Wie alt ist der Chef?", "frageUz": "Rahbar necha yoshda?", "richtig": "fünfzig", "falsch": ["vierzig", "sechzig"] },
    { "frageDe": "Wie ist der Chef?", "frageUz": "Rahbar qanday odam?", "richtig": "lustig", "falsch": ["müde", "alt"] }
   ]
```
`u02-d6`:
```json
   "fragen": [
    { "frageDe": "Wer ist der Mann?", "frageUz": "Erkak kim?", "richtig": "der Freund", "falsch": ["der Kollege", "der Bruder"] },
    { "frageDe": "Was macht der Freund?", "frageUz": "Do'sti nima qiladi?", "richtig": "Er studiert in Deutschland.", "falsch": ["Er arbeitet in Deutschland.", "Er wohnt in Usbekistan."] }
   ]
```

- [ ] **Step 4: Qo'riqchini yuriting**

Run: `npx jest src/daf/inhalt --silent 2>&1 | grep -E "●.*›|Tests:"; npm run daf:inhalt-check -- --unit 1 | tail -2; npm run daf:inhalt-check -- --unit 2 | tail -2`
Expected: hamma test o'tadi; ikkala unit «matn toza». Notanish so'z chiqsa — savolni o'zgartiring, ro'yxatni emas (Task 2 qoidasi: `hilfswoerter` ga faqat atoqli ot va grammatik ko'makchi kiradi).

- [ ] **Step 5: Lokal seed**

Run: `export DATABASE_URL=...; npx ts-node scripts/daf-inhalt-seed.ts --unit 1 | tail -3; npx ts-node scripts/daf-inhalt-seed.ts --unit 2 | tail -3`
Expected: ikkala hisobotda `"hoerFragen": 12`.

- [ ] **Step 6: Commit**

```bash
git add content/daf/a1/hilfswoerter.json content/daf/a1/u01/dialoge.json content/daf/a1/u02/dialoge.json
git commit -m "1 va 2-unit dialoglariga 24 ta eshitish savoli

Har savol o'z bo'limigacha o'rgatilgan so'zlardangina: u01-s1 da
faqat «wie» so'roq so'zi bor, shuning uchun u yerda savollar «Ist das
Mia, Anna oder Claudia?» shaklida. Chalg'ituvchilar suhbatning o'zidan
(aka uchta bola, Jonas charchagan) — to'g'ri javob hech qachon ikkita
bo'lmaydi.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: Dvigatel tiplari va `itemType` ning kompilyator qo'riqchisi

**Files:**
- Modify: `server/src/daf/uebung/frage.types.ts`
- Modify: `server/src/daf/dto/uebung.dto.ts` (`CheckAntwortDto.itemType`, `ErsatzQueryDto.itemType`)
- Modify: `server/src/daf/dto/uebung.dto.spec.ts`

**Interfaces:**
- Produces:
  - `export type ItemType = 'WORT' | 'SATZ' | 'PHRASE' | 'DIALOGZEILE' | 'HOERFRAGE'`
  - `FrageFormat` ga `'HOEREN_WAHL'`
  - `interface MaterialHoerFrage { id: number; frageDe: string; frageUz: string; richtig: string; falsch: string[] }`
  - `MaterialDialog.audioKey: string | null; MaterialDialog.fragen: MaterialHoerFrage[]`
  - `export const ITEM_TYPEN: ItemType[]` (DTO)

- [ ] **Step 1: Yiqiladigan DTO testini yozing**

`uebung.dto.spec.ts` ga:

```ts
import { FRAGE_FORMATLAR, ITEM_TYPEN } from './uebung.dto';

describe('ITEM_TYPEN', () => {
  it("HOERFRAGE ro'yxatda bor — @IsIn simi eshitish javobini rad etmaydi", () => {
    // Ilgari bu ro'yxat ikki DTO'da QO'LDA yozilgan edi; unutilsa har
    // eshitish javobi 400 bilan qaytardi — aynan AUDIO_WORT bilan
    // bo'lgan xato, faqat itemType tomonida.
    expect(ITEM_TYPEN).toEqual(
      expect.arrayContaining(['WORT', 'SATZ', 'PHRASE', 'DIALOGZEILE', 'HOERFRAGE']),
    );
  });
});
```

- [ ] **Step 2: Testni yuritib yiqilishini tasdiqlang**

Run: `npx jest src/daf/dto/uebung.dto.spec.ts`
Expected: FAIL — `ITEM_TYPEN` eksport qilinmagan.

- [ ] **Step 3: `frage.types.ts` ni kengaytiring**

`FrageFormat` ittifoqiga `| 'HOEREN_WAHL'`. `MaterialDialog` dan OLDIN:

```ts
/**
 * Savol o'zligining birinchi yarmi — material turi. `Frage.itemType`,
 * DTO'lar, `ladeMaterial` va mijozning `MaterialTyp`i shu BITTA tipdan
 * yuradi: yangi tur qo'shilib bittasida unutilsa kompilyator yiqitadi
 * (`uebung.dto.ts`dagi `Record<ItemType, true>`).
 */
export type ItemType = 'WORT' | 'SATZ' | 'PHRASE' | 'DIALOGZEILE' | 'HOERFRAGE';

/** Suhbatni eshitgandan keyingi tushunish savoli — `HOEREN_WAHL` materiali. */
export interface MaterialHoerFrage {
  id: number;
  frageDe: string;
  frageUz: string;
  richtig: string;
  falsch: string[];
}
```

`MaterialDialog`:

```ts
export interface MaterialDialog {
  id: number;
  titelDe: string;
  zeilen: MaterialDialogZeile[];
  sectionCode: string;
  /** Butun suhbatning R2 kaliti; `null` — ovoz yo'q, eshitish savoli qurilmaydi. */
  audioKey: string | null;
  fragen: MaterialHoerFrage[];
}
```

`Frage.itemType` va `PublicFrage.itemType` ni `ItemType` ga almashtiring (`itemType: ItemType;`). `materialSchluessel(itemType: ItemType, ...)`.

- [ ] **Step 4: DTO'ni `Record` ga o'tkazing**

`uebung.dto.ts` da `import type { FrageFormat, ItemType } from '../uebung/frage.types';`. `FRAGE_FORMATLAR` dan keyin:

```ts
/**
 * `ItemType`ning HAR BIR a'zosi — `FrageFormat` bilan bir xil sabab:
 * ro'yxat ikki DTO'da qo'lda yozilgan edi va yangi tur (`HOERFRAGE`)
 * unutilsa har javob `@IsIn` da 400 bilan qaytardi.
 */
const ALLE_ITEM_TYPEN: Record<ItemType, true> = {
  WORT: true,
  SATZ: true,
  PHRASE: true,
  DIALOGZEILE: true,
  HOERFRAGE: true,
};

export const ITEM_TYPEN = Object.keys(ALLE_ITEM_TYPEN) as ItemType[];
```

`CheckAntwortDto` va `ErsatzQueryDto` da:

```ts
  @IsIn(ITEM_TYPEN)
  itemType!: ItemType;
```

(`JuftDto` tegilmaydi — u ataylab tor.)

- [ ] **Step 5: Kompilyator ko'rsatgan joylarni to'g'rilang**

Run: `npm run typecheck 2>&1 | grep error | head`
Expected: `uebung.service.ts` da `PruefenInput.itemType`, `ersatz(... itemType: ...)`, `ladeMaterial(itemType: ...)` va `toDialog` (`audioKey`/`fragen` yetishmaydi), `daf-media-fragen.service.ts` da `dialoge` map — xatolar. Ularni shunday yoping:
- `uebung.service.ts`: `PruefenInput.itemType: ItemType`, `ersatz(..., itemType: ItemType, ...)`, `ladeMaterial(itemType: ItemType, ...)` — `ItemType` ni `frage.types`dan import qiling. `toDialog` ga hozircha `audioKey: null, fragen: []` (Task 8 to'ldiradi).
- `daf-media-fragen.service.ts` dialog map ga `audioKey: null, fragen: []` (Task 9 to'ldiradi).
- `uebung.service.spec.ts` `fakePrisma` `dialog` qatorlariga `audioKey: null, fragen: []` (Task 8 haqiqiy qiymat beradi).

Run: `npm run typecheck`
Expected: toza.

- [ ] **Step 6: Testlar va commit**

Run: `npx jest src/daf --silent 2>&1 | tail -4`
Expected: PASS (barcha).

```bash
git add src/daf/uebung/frage.types.ts src/daf/dto/uebung.dto.ts src/daf/dto/uebung.dto.spec.ts src/daf/uebung/uebung.service.ts src/daf/uebung/uebung.service.spec.ts src/daf/media/daf-media-fragen.service.ts
git commit -m "itemType kompilyator qo'riqlaydigan tipga chiqdi; HOEREN_WAHL va HOERFRAGE tiplari

Ro'yxat ikki DTO'da qo'lda yozilgan edi — Record<ItemType, true> bilan
unutish endi typecheck xatosi. MaterialDialog audioKey va fragen oladi.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 6: `DIALOG_LUECKE` hamma satrni band qiladi; `hoerenWahl()` quruvchisi

**Files:**
- Modify: `server/src/daf/uebung/dialog-fragen.ts` (`belegteItems`, ~148)
- Modify: `server/src/daf/uebung/dialog-fragen.spec.ts`
- Create: `server/src/daf/uebung/hoer-fragen.ts`
- Create: `server/src/daf/uebung/hoer-fragen.spec.ts`

**Interfaces:**
- Consumes: `MaterialDialog` (Task 5), `materialSchluessel`, `mischen` (dialog-fragen.ts ichida — eksport qilinadi), `MediaUrlResolver` (`wort-fragen.ts`: `(key: string) => string | null`).
- Produces: `hoerenWahl(dialog: MaterialDialog, rnd: () => number, mediaUrl: MediaUrlResolver): Frage | null`; `dialogBelegt(dialog: MaterialDialog): string[]`.

- [ ] **Step 1: Yiqiladigan testlar — `DIALOG_LUECKE`**

`dialog-fragen.spec.ts` ga:

```ts
  it('suhbatning HAMMA satrini band qiladi — bir seansda ikki marta chiqmasin', () => {
    // Faqat olib tashlangan satr band bo'lsa, o'sha suhbatning ikkinchi
    // bo'sh joyi (yoki eshitish savoli) bitta seansga tushardi — va
    // ikkinchisi birinchisining javobini ko'rsatib turardi.
    const f = dialogLuecke(dialog, andere, rndId)!;
    expect(f.belegteItems.sort()).toEqual(
      ['DIALOGZEILE:10', 'DIALOGZEILE:11', 'DIALOGZEILE:12', 'DIALOGZEILE:13'],
    );
  });
```

- [ ] **Step 2: Yiqiladigan testlar — `hoerenWahl`**

`server/src/daf/uebung/hoer-fragen.spec.ts`:

```ts
import { hoerenWahl } from './hoer-fragen';
import type { MaterialDialog } from './frage.types';

const dialog = (): MaterialDialog => ({
  id: 5,
  titelDe: 'Meine Schwester',
  sectionCode: 'u02-s1',
  audioKey: 'daf/audio/abc.mp3',
  zeilen: [
    { id: 50, sprecher: 'Anna', de: 'Ist das deine Schwester?', uz: 'x' },
    { id: 51, sprecher: 'Jonas', de: 'Nein, das ist meine Tochter.', uz: 'x' },
    { id: 52, sprecher: 'Anna', de: 'Wo wohnen sie?', uz: 'x' },
    { id: 53, sprecher: 'Jonas', de: 'In Deutschland.', uz: 'x' },
  ],
  fragen: [
    { id: 71, frageDe: 'Wer ist Nodira?', frageUz: 'Nodira kim?', richtig: 'die Tochter', falsch: ['die Schwester', 'die Mutter'] },
    { id: 72, frageDe: 'Wo wohnen die Eltern?', frageUz: 'Qayerda?', richtig: 'in Deutschland', falsch: ['in Usbekistan', 'hier'] },
  ],
});

const mediaUrl = (key: string) => `https://r2.example/${key}`;

describe('hoerenWahl', () => {
  it('savol quradi: prompt — nemischa savol, hilfe — o`zbekchasi, 3 variant', () => {
    const f = hoerenWahl(dialog(), () => 0, mediaUrl)!;
    expect(f).not.toBeNull();
    expect(f.format).toBe('HOEREN_WAHL');
    expect(f.itemType).toBe('HOERFRAGE');
    expect(f.itemId).toBe(71);
    expect(f.prompt).toBe('Wer ist Nodira?');
    expect(f.hilfe).toBe('Nodira kim?');
    expect(f.options).toHaveLength(3);
    expect(f.options).toContain('die Tochter');
    expect(f.richtig).toBe('die Tochter');
    expect(f.audioUrl).toBe('https://r2.example/daf/audio/abc.mp3');
    expect(f.titel).toBe('Meine Schwester');
  });

  it('rnd savolni tanlaydi', () => {
    expect(hoerenWahl(dialog(), () => 0.99, mediaUrl)!.itemId).toBe(72);
  });

  it('variantlar aralashtiriladi — to`g`ri javob doim birinchi emas', () => {
    const joylar = new Set<number>();
    for (let i = 0; i < 20; i += 1) {
      const f = hoerenWahl(dialog(), () => i / 20, mediaUrl)!;
      joylar.add(f.options.indexOf(f.richtig));
    }
    expect(joylar.size).toBeGreaterThan(1);
  });

  it('suhbat matni promptda YO`Q — eshitish, o`qish emas', () => {
    const f = hoerenWahl(dialog(), () => 0, mediaUrl)!;
    expect(f.prompt).not.toContain('Tochter.');
    expect(JSON.stringify(f)).not.toContain('Nein, das ist meine Tochter');
  });

  it('suhbatning HAMMA satrini band qiladi', () => {
    const f = hoerenWahl(dialog(), () => 0, mediaUrl)!;
    expect(f.belegteItems.sort()).toEqual([
      'DIALOGZEILE:50', 'DIALOGZEILE:51', 'DIALOGZEILE:52', 'DIALOGZEILE:53',
      'HOERFRAGE:71',
    ]);
  });

  it('audioKey yo`q — savol qurilmaydi', () => {
    expect(hoerenWahl({ ...dialog(), audioKey: null }, () => 0, mediaUrl)).toBeNull();
  });

  it('R2_PUBLIC_URL sozlanmagan (resolver null) — savol qurilmaydi', () => {
    expect(hoerenWahl(dialog(), () => 0, () => null)).toBeNull();
  });

  it('savoli yo`q dialogdan savol qurilmaydi', () => {
    expect(hoerenWahl({ ...dialog(), fragen: [] }, () => 0, mediaUrl)).toBeNull();
  });
});
```

- [ ] **Step 3: Yiqilishini tasdiqlang**

Run: `npx jest src/daf/uebung/dialog-fragen.spec.ts src/daf/uebung/hoer-fragen.spec.ts 2>&1 | grep -E "●.*›|Tests:"`
Expected: `belegteItems` testi va hoer-fragen suite (modul yo'q) yiqiladi.

- [ ] **Step 4: `dialog-fragen.ts` ni o'zgartiring**

`mischen` ni eksport qiling (`export function mischen`). Yangi eksport (`dialogLuecke` dan OLDIN):

```ts
/**
 * Suhbatning HAMMA satri — `belegteItems` uchun.
 *
 * `DIALOG_LUECKE` ilgari faqat olib tashlangan satrni band qilardi,
 * ya'ni (a) bitta suhbatning ikki xil bo'sh joyi bir seansga tushib,
 * ikkinchisi birinchisining javobini ko'rsatib turardi; (b) eshitish
 * savoli (`HOEREN_WAHL`) bilan o'sha suhbatning matnli savoli bitta
 * seansga tushib, o'quvchi avval matnni O'QIB, keyin «eshitib» javob
 * berardi. Seans quruvchisining 4-qoidasi (`belegteItems` kesishmasin)
 * ikkalasini bir yo'la yopadi — agar ikkala format ham BUTUN suhbatni
 * band qilsa.
 */
export function dialogBelegt(dialog: MaterialDialog): string[] {
  return dialog.zeilen.map((z) => materialSchluessel('DIALOGZEILE', z.id));
}
```

`dialogLuecke` qaytaradigan obyektda:

```ts
    belegteItems: dialogBelegt(dialog),
```

- [ ] **Step 5: `hoer-fragen.ts` ni yozing**

```ts
import { dialogBelegt, mischen } from './dialog-fragen';
import type { MediaUrlResolver } from './wort-fragen';
import {
  materialSchluessel,
  type Frage,
  type MaterialDialog,
} from './frage.types';

/**
 * `HOEREN_WAHL` — suhbatni eshitib, tushunish savoliga javob berish.
 *
 * SUHBAT MATNI SAVOLDA YO'Q: `prompt` — savolning o'zi (`frageDe`),
 * `hilfe` — o'zbekchasi, `audioUrl` — butun suhbat. Matn faqat javobdan
 * keyin (`pruefen` → `transkript`) ochiladi — aks holda bu o'qish
 * mashqi bo'lib qolardi.
 *
 * AUDIOSIZ QURILMAYDI (`audioKey` yoki resolver `null`): so'z audiosidagi
 * qoida — `R2_PUBLIC_URL` sozlanmagan bazada xom kalit sizmaydi,
 * format ovoz yasalmaguncha o'z-o'zidan o'chiq turadi.
 *
 * `belegteItems` suhbatning HAMMA satrini oladi (`dialogBelegt`) —
 * `DIALOG_LUECKE` bilan bir seansga tushmasin.
 */
export function hoerenWahl(
  dialog: MaterialDialog,
  rnd: () => number,
  mediaUrl: MediaUrlResolver,
): Frage | null {
  if (!dialog.audioKey || dialog.fragen.length === 0) return null;
  const audioUrl = mediaUrl(dialog.audioKey);
  if (!audioUrl) return null;

  const frage = dialog.fragen[Math.floor(rnd() * dialog.fragen.length)];

  return {
    format: 'HOEREN_WAHL',
    itemType: 'HOERFRAGE',
    itemId: frage.id,
    prompt: frage.frageDe,
    hilfe: frage.frageUz,
    options: mischen([frage.richtig, ...frage.falsch], rnd),
    richtig: frage.richtig,
    akzeptiert: [],
    belegteItems: [
      ...dialogBelegt(dialog),
      materialSchluessel('HOERFRAGE', frage.id),
    ],
    titel: dialog.titelDe,
    audioUrl,
  };
}
```

`MediaUrlResolver` `wort-fragen.ts` da eksport qilinganini tekshiring (`grep -n "MediaUrlResolver" src/daf/uebung/wort-fragen.ts`); bo'lmasa `export type MediaUrlResolver = (key: string) => string | null;` qo'shing.

- [ ] **Step 6: O'tishini tasdiqlang, commit**

Run: `npx jest src/daf/uebung --silent 2>&1 | tail -4 && npm run typecheck`
Expected: PASS, toza.

```bash
git add src/daf/uebung/dialog-fragen.ts src/daf/uebung/dialog-fragen.spec.ts src/daf/uebung/hoer-fragen.ts src/daf/uebung/hoer-fragen.spec.ts src/daf/uebung/wort-fragen.ts
git commit -m "hoerenWahl quruvchisi; dialog formatlari suhbatning hamma satrini band qiladi

DIALOG_LUECKE faqat olib tashlangan satrni band qilardi — bitta
suhbatning ikki bo'sh joyi yoki eshitish savoli bilan matnli savoli
bir seansga tushib, biri ikkinchisining javobini ko'rsatardi.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 7: Seansda ko'pi bilan bitta eshitish savoli; `UNIT_TEST` moyilligi

**Files:**
- Modify: `server/src/daf/uebung/seans.ts`
- Modify: `server/src/daf/uebung/seans.spec.ts`
- Modify: `server/src/daf/uebung/kind-formate.ts`
- Modify: `server/src/daf/uebung/kind-formate.spec.ts`

**Interfaces:**
- Produces: `export const FORMAT_CAP: Partial<Record<FrageFormat, number>> = { HOEREN_WAHL: 1 }`; `export function capFuer(format: FrageFormat): number`.

- [ ] **Step 1: Yiqiladigan testlar**

`seans.spec.ts` ga (`describe('baueSeans'` ichiga):

```ts
  it('HOEREN_WAHL seansda ko`pi bilan bitta — har biri 30–60 soniya', () => {
    const hoeren = Array.from({ length: 5 }, (_, j) =>
      f('HOEREN_WAHL', 900 + j, [`HOERFRAGE:${900 + j}`]),
    );
    const { fragen } = baueSeans([...hoeren, ...kandidaten()], 12, rnd);
    expect(fragen.filter((q) => q.format === 'HOEREN_WAHL')).toHaveLength(1);
    expect(fragen).toHaveLength(12);
  });

  it('boshqa formatlar uchun umumiy chegara o`zgarmaydi', () => {
    expect(capFuer('WORT_UZ')).toBe(FORMAT_MAX_PRO_SEANS);
    expect(capFuer('HOEREN_WAHL')).toBe(1);
  });
```

Import qatoriga `capFuer` qo'shing.

`kind-formate.spec.ts` da `barchasi` ro'yxatiga `'HOEREN_WAHL'`, va «Yakuniy sinov vaziyatga suyanadi» testida `arrayContaining` ga `'HOEREN_WAHL'`.

- [ ] **Step 2: Yiqilishini tasdiqlang**

Run: `npx jest src/daf/uebung/seans.spec.ts src/daf/uebung/kind-formate.spec.ts 2>&1 | grep -E "●.*›|Tests:"`
Expected: 3 test yiqiladi.

- [ ] **Step 3: `seans.ts`**

`MIN_FORMATE` dan keyin:

```ts
/**
 * Format bo'yicha ALOHIDA chegara — umumiy `FORMAT_MAX_PRO_SEANS`
 * ustidan. `HOEREN_WAHL` bittasi 30–60 soniya oladi: uchtasi seansni
 * uch daqiqa uzaytirardi. Yozilmagan format umumiy chegarani oladi.
 */
export const FORMAT_CAP: Partial<Record<FrageFormat, number>> = {
  HOEREN_WAHL: 1,
};

export function capFuer(format: FrageFormat): number {
  return FORMAT_CAP[format] ?? FORMAT_MAX_PRO_SEANS;
}
```

`baueSeans` ichidagi UCHTA `(proFormat.get(f.format) ?? 0) >= FORMAT_MAX_PRO_SEANS` / `< FORMAT_MAX_PRO_SEANS` solishtiruvini `capFuer(f.format)` bilan almashtiring (majburiy siklda `capToldi`, bo'shliq qidiruvida `spacerIdx`, 2-bosqichda `mosKeladi`).

- [ ] **Step 4: `kind-formate.ts`**

```ts
  UNIT_TEST: ['REAKTION', 'ZUORDNEN', 'DIALOG_LUECKE', 'SATZ_UEBERSETZEN', 'HOEREN_WAHL'],
```

- [ ] **Step 5: O'tishini tasdiqlang, commit**

Run: `npx jest src/daf/uebung --silent 2>&1 | tail -4 && npm run typecheck`

```bash
git add src/daf/uebung/seans.ts src/daf/uebung/seans.spec.ts src/daf/uebung/kind-formate.ts src/daf/uebung/kind-formate.spec.ts
git commit -m "Seansda ko'pi bilan bitta eshitish savoli; UNIT_TEST uni afzal ko'radi

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 8: Dvigatel — material, nomzod, `pruefen`, `transkript`

**Files:**
- Modify: `server/src/daf/uebung/uebung.service.ts` (`ladeMaterial` ~1361, `richtigeAntwort` ~146, `baueKandidaten` dialog qismi ~600–770, `pruefen` ~890–1032, `PruefenErgebnis` ~220)
- Modify: `server/src/daf/uebung/uebung.service.spec.ts`

**Interfaces:**
- Consumes: `hoerenWahl` (Task 6), `ItemType`/`MaterialHoerFrage` (Task 5), Prisma `dafHoerFrage`.
- Produces: `PruefenErgebnis.transkript?: Array<{ sprecher: string; de: string; uz: string }>`; `ladeMaterial('HOERFRAGE', id)` → `{ de: richtig, uz: frageUz, dialogId }`.

- [ ] **Step 1: Yiqiladigan testlar**

`uebung.service.spec.ts` `fakePrisma()` da `dialog[0]` ga `audioKey: 'daf/audio/d1.mp3'` va `fragen: [{ id: 401, frageDe: 'Wie geht es A?', frageUz: 'A qalay?', richtig: 'gut', falsch: ['nicht gut', 'sehr gut'] }]`, `dialog[1]` ga `audioKey: null, fragen: []`. `dafHoerFrage` modeli:

```ts
    dafHoerFrage: {
      findUnique: jest.fn(async ({ where }: any) => {
        for (const d of dialog) {
          const f = d.fragen.find((x: any) => x.id === where.id);
          if (f) return { ...f, dialogId: d.id };
        }
        return null;
      }),
    },
```

`config` — spec da `UebungService` qanday quriladi, tekshiring (`new UebungService(prisma as any)` — ikkinchi argument `config` ixtiyoriy, `mediaUrl` `this.config?.get` bilan). `R2_PUBLIC_URL` beradigan soxta config yarating: `const config = { get: (k: string) => (k === 'R2_PUBLIC_URL' ? 'https://r2.example' : undefined) };` va eshitish testlarida `new UebungService(prisma as any, config as any)`.

Yangi `describe('HOEREN_WAHL'`:

```ts
describe('HOEREN_WAHL', () => {
  const ctx = { studentId: 55, companyId: 1 };
  const config = {
    get: (k: string) => (k === 'R2_PUBLIC_URL' ? 'https://r2.example' : undefined),
  };

  it('audiosi va savoli bor dialogdan nomzod quriladi, audiosiz dialogdan emas', async () => {
    const prisma = fakePrisma();
    const svc = new UebungService(prisma as any, config as any);
    const fragen = await svc.seans(100, 55, () => 0.5);
    const hoeren = fragen.filter((f) => f.format === 'HOEREN_WAHL');
    expect(hoeren.length).toBeLessThanOrEqual(1);
    if (hoeren.length === 1) {
      expect(hoeren[0].itemType).toBe('HOERFRAGE');
      expect(hoeren[0].itemId).toBe(401);
      expect(hoeren[0].audioUrl).toBe('https://r2.example/daf/audio/d1.mp3');
      expect((hoeren[0] as any).richtig).toBeUndefined();
    }
  });

  it('config yo`q (R2_PUBLIC_URL sozlanmagan) — eshitish savoli qurilmaydi', async () => {
    const prisma = fakePrisma();
    const fragen = await new UebungService(prisma as any).seans(100, 55, () => 0.5);
    expect(fragen.filter((f) => f.format === 'HOEREN_WAHL')).toEqual([]);
  });

  it('pruefen: to`g`ri javob, transkript faqat javobdan keyin', async () => {
    const prisma = fakePrisma();
    const r = await new UebungService(prisma as any, config as any).pruefen(
      { itemType: 'HOERFRAGE', itemId: 401, format: 'HOEREN_WAHL', given: 'gut' },
      ctx,
    );
    expect(r.isCorrect).toBe(true);
    expect(r.richtig).toBe('gut');
    expect(r.transkript).toEqual([
      { sprecher: 'A', de: 'Hallo!', uz: 'Salom!' },
      { sprecher: 'B', de: 'Hallo, wie geht es dir?', uz: 'Salom, ahvoling qalay?' },
      { sprecher: 'A', de: 'Gut, danke.', uz: 'Yaxshi, rahmat.' },
      { sprecher: 'B', de: 'Bis bald!', uz: "Ko'rishguncha!" },
    ]);
  });

  it('pruefen: xato javob ham transkriptni ochadi, ball yo`q, Leitner yo`q', async () => {
    const prisma = fakePrisma();
    const r = await new UebungService(prisma as any, config as any).pruefen(
      { itemType: 'HOERFRAGE', itemId: 401, format: 'HOEREN_WAHL', given: 'nicht gut' },
      ctx,
    );
    expect(r.isCorrect).toBe(false);
    expect(r.transkript).toHaveLength(4);
    // Ball va Leitner — mavjud `itemType !== 'WORT'` qoidasi: urinish
    // yoziladi (0 ball, lexemeId null), holat jadvaliga tegilmaydi.
    const attempt = prisma.dafAttempt.create.mock.calls[0][0].data;
    expect(attempt.points).toBe(0);
    expect(attempt.lexemeId).toBeNull();
    expect(prisma.dafLexemeState.upsert).not.toHaveBeenCalled();
  });

  it('boshqa formatda transkript YO`Q', async () => {
    const prisma = fakePrisma();
    const r = await new UebungService(prisma as any, config as any).pruefen(
      { itemType: 'WORT', itemId: 1, format: 'WORT_UZ', given: 'salom' },
      ctx,
    );
    expect(r.transkript).toBeUndefined();
  });

  it('ersatz: eshitish savoliga o`rinbosar yo`q — null', async () => {
    // Mijoz `null` ni «savol tugatildi» deb oladi (`ersatzKeldi`) —
    // xato javob qaytmaydi, yangi kod yo'q.
    const prisma = fakePrisma();
    const e = await new UebungService(prisma as any, config as any).ersatz(
      100, 55, 'HOERFRAGE', 401, 'HOEREN_WAHL', () => 0.5,
    );
    expect(e).toBeNull();
  });

  it('itemType mos kelmasa rad etadi', async () => {
    const prisma = fakePrisma();
    await expect(
      new UebungService(prisma as any, config as any).pruefen(
        { itemType: 'WORT', itemId: 1, format: 'HOEREN_WAHL', given: 'gut' },
        ctx,
      ),
    ).rejects.toThrow('HOEREN_WAHL savoli faqat eshitish savoliga tegishli');
  });
});
```

`fakePrisma` da `dafAttempt.create` va `dafLexemeState.upsert` oddiy `jest.fn` — yuqoridagi testlar ularning `mock.calls` ini o'qiydi, boshqa hech narsa kerak emas.

- [ ] **Step 2: Yiqilishini tasdiqlang**

Run: `npx jest src/daf/uebung/uebung.service.spec.ts -t HOEREN_WAHL 2>&1 | grep -E "●.*›|Tests:"`
Expected: hammasi yiqiladi.

- [ ] **Step 3: `ladeMaterial` ga `HOERFRAGE` shoxi**

`ladeMaterial` qaytish tipiga `dialogId?: number` qo'shing. `DIALOGZEILE` shoxidan keyin, iboradan OLDIN:

```ts
    if (itemType === 'HOERFRAGE') {
      // `de` — to'g'ri javob (`richtigeAntwort` shu maydonni o'qiydi),
      // `uz` — savolning o'zbekchasi (hech qayerda solishtirilmaydi).
      // `dialogId` — javobdan keyin transkriptni ochish uchun.
      const row = (await this.prisma.dafHoerFrage.findUnique({
        where: { id: itemId },
      } as any)) as {
        richtig: string;
        frageUz: string;
        dialogId: number;
      } | null;
      return row ? { de: row.richtig, uz: row.frageUz, dialogId: row.dialogId } : null;
    }
```

- [ ] **Step 4: `richtigeAntwort` ga holat**

`DIALOG_LUECKE` holatidan keyin:

```ts
    case 'HOEREN_WAHL':
      // `ladeMaterial` `de`ga `DafHoerFrage.richtig`ni qo'yadi.
      // `akzeptiert` bo'sh: variantlar aynan, yozish yo'q.
      return { richtig: material.de, akzeptiert: [] };
```

- [ ] **Step 5: `pruefen` — itemType qorovuli va transkript**

`PruefenErgebnis`:

```ts
export interface PruefenErgebnis {
  isCorrect: boolean;
  richtig: string;
  /**
   * FAQAT `HOEREN_WAHL` — suhbat matni javobdan KEYIN ochiladi
   * (o'quvchi nimani eshitmaganini ko'radi). Boshqa formatda `undefined`,
   * savol bilan birga hech qachon yuborilmaydi (D6/D7).
   */
  transkript?: Array<{ sprecher: string; de: string; uz: string }>;
}
```

`pruefen` da `ladeMaterial` dan KEYIN, `PAAR` shoxidan OLDIN:

```ts
    if (format === 'HOEREN_WAHL' && itemType !== 'HOERFRAGE') {
      // `PAAR`/`ZUORDNEN` qorovuli bilan bir xil sabab: DTO `itemType` va
      // `format`ni mustaqil tekshiradi.
      throw new BadRequestException(
        'HOEREN_WAHL savoli faqat eshitish savoliga tegishli',
      );
    }
```

Funksiya oxirida `return { isCorrect, richtig };` o'rniga:

```ts
    if (format === 'HOEREN_WAHL' && material.dialogId != null) {
      const zeilen = (await this.prisma.dafDialogLine.findMany({
        where: { dialogId: material.dialogId },
        orderBy: { order: 'asc' },
        select: { sprecher: true, de: true, uz: true },
      } as any)) as Array<{ sprecher: string; de: string; uz: string }>;
      return { isCorrect, richtig, transkript: zeilen };
    }
    return { isCorrect, richtig };
```

`fakePrisma.dafDialogLine` ga `findMany` qo'shing (spec): `findMany: jest.fn(async ({ where }: any) => dialog.find((d) => d.id === where.dialogId)?.zeilen.map((z) => ({ sprecher: z.sprecher, de: z.de, uz: z.uz })) ?? [])`.

- [ ] **Step 6: `baueKandidaten` — dialogga `audioKey`/`fragen`, nomzod**

`dafDialog.findMany` ga `include: { zeilen: { orderBy: { order: 'asc' } }, fragen: { orderBy: { order: 'asc' } } }`. `DialogRow` ga `audioKey: string | null; fragen: Array<{ id: number; frageDe: string; frageUz: string; richtig: string; falsch: string[] }>`. `toDialog` da `audioKey: d.audioKey, fragen: d.fragen.map((f) => ({ id: f.id, frageDe: f.frageDe, frageUz: f.frageUz, richtig: f.richtig, falsch: f.falsch }))` (Task 5 dagi `audioKey: null, fragen: []` ni almashtiring).

`dialogLuecke` siklidan keyin:

```ts
    // Eshitish savoli — audiosi va savoli bor har dialog uchun bitta
    // nomzod. `hoerenWahl` audiosiz dialogda `null` qaytaradi, ya'ni
    // ovoz yasalmaguncha format o'z-o'zidan o'chiq.
    for (const d of dialoge) {
      const hw = hoerenWahl(d, rnd, (key) => this.mediaUrl(key));
      if (hw) rohKandidaten.push(hw);
    }
```

Import: `import { hoerenWahl } from './hoer-fragen';`.

- [ ] **Step 7: Testlar, typecheck, lint, commit**

Run: `npx jest src/daf --silent 2>&1 | tail -4 && npm run typecheck && npx eslint src/daf/uebung 2>&1 | tail -1`

```bash
git add src/daf/uebung/uebung.service.ts src/daf/uebung/uebung.service.spec.ts
git commit -m "Dvigatel: HOEREN_WAHL nomzodi, tekshiruvi va javobdan keyingi transkript

Transkript faqat HOEREN_WAHL da va faqat javobdan keyin; ball va
Leitner mavjud itemType !== 'WORT' qoidasi bilan o'z-o'zidan yo'q.
ersatz null qaytaradi — xato javob qaytmaydi, yangi kod yo'q.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 9: `/media` — savollar ko'rigi va dialog ro'yxati (server)

**Files:**
- Modify: `server/src/daf/media/daf-media-fragen.service.ts` (`VORSCHAU_BAUER`, `dafDialog.findMany` ~262, dialog map ~330)
- Modify: `server/src/daf/media/daf-media-inhalt.service.ts` (`SectionInhalt`, `inhalt()`)
- Modify: `server/src/daf/media/daf-media-inhalt.service.spec.ts`
- Modify: `server/src/daf/media/daf-media-fragen.service.spec.ts` (agar u `VORSCHAU_BAUER` kalitlarini sanasa — `HOEREN_WAHL` qo'shing)

**Interfaces:**
- Consumes: `hoerenWahl` (Task 6), Prisma `dafDialog.fragen`.
- Produces: `SectionInhalt.dialoge: InhaltDialog[]` — `interface InhaltDialog { id: number; code: string; titelDe: string; titelUz: string; audioUrl: string | null; fragen: Array<{ code: string; frageDe: string; frageUz: string; richtig: string; falsch: string[] }> }`.

- [ ] **Step 1: Yiqiladigan test — inhalt**

`daf-media-inhalt.service.spec.ts` `fakePrisma` ga `dafDialog: { findMany: jest.fn(async () => rows.dialoge ?? []) }`. Yangi test:

```ts
  it('dialoglarni audio manzili va savollari bilan qaytaradi', async () => {
    const p = fakePrisma({
      dialoge: [
        {
          id: 9,
          code: 'u02-d2',
          titelDe: 'Meine Schwester',
          titelUz: 'Mening opam',
          audioKey: 'daf/audio/d.mp3',
          fragen: [
            { code: 'u02-d2-f1', frageDe: 'Wer ist Nodira?', frageUz: 'Nodira kim?', richtig: 'die Tochter', falsch: ['die Schwester', 'die Mutter'] },
          ],
        },
      ],
    });
    const r = await new DafMediaInhaltService(p as any, config as any).inhalt(7);
    expect(r.dialoge).toEqual([
      {
        id: 9,
        code: 'u02-d2',
        titelDe: 'Meine Schwester',
        titelUz: 'Mening opam',
        audioUrl: 'https://r2.example/daf/audio/d.mp3',
        fragen: [
          { code: 'u02-d2-f1', frageDe: 'Wer ist Nodira?', frageUz: 'Nodira kim?', richtig: 'die Tochter', falsch: ['die Schwester', 'die Mutter'] },
        ],
      },
    ]);
  });
```

- [ ] **Step 2: Yiqilishini tasdiqlang**

Run: `npx jest src/daf/media/daf-media-inhalt.service.spec.ts 2>&1 | grep -E "●.*›|Tests:"`

- [ ] **Step 3: `daf-media-inhalt.service.ts`**

`InhaltDialogZeile` dan keyin:

```ts
/** Butun dialog — CEO ovozini shu yerda eshitadi, savollarini shu yerda ko'radi. */
export interface InhaltDialog {
  id: number;
  code: string;
  titelDe: string;
  titelUz: string;
  audioUrl: string | null;
  fragen: Array<{
    code: string;
    frageDe: string;
    frageUz: string;
    richtig: string;
    falsch: string[];
  }>;
}
```

`SectionInhalt` ga `dialoge: InhaltDialog[];`. `Promise.all` ga beshinchi so'rov:

```ts
        this.prisma.dafDialog.findMany({
          where: { sectionId },
          orderBy: { id: 'asc' },
          include: { fragen: { orderBy: { order: 'asc' } } },
        } as any),
```

(destructuringga `dialogRows` qo'shing.) Map:

```ts
    const dialoge: InhaltDialog[] = (dialogRows as any[]).map((d) => ({
      id: d.id,
      code: d.code,
      titelDe: d.titelDe,
      titelUz: d.titelUz,
      audioUrl: this.mediaUrl(d.audioKey),
      fragen: (d.fragen as any[]).map((f) => ({
        code: f.code,
        frageDe: f.frageDe,
        frageUz: f.frageUz,
        richtig: f.richtig,
        falsch: f.falsch,
      })),
    }));
```

va qaytariladigan obyektga `dialoge`.

- [ ] **Step 4: `daf-media-fragen.service.ts`**

Import `hoerenWahl`. `VORSCHAU_BAUER` ga (`DIALOG_LUECKE` dan keyin):

```ts
  HOEREN_WAHL: (m, r) =>
    m.dialoge.map((d) => hoerenWahl(d, r, m.mediaUrl)).filter(nichtNull),
```

`dafDialog.findMany` include ga `fragen: { orderBy: { order: 'asc' } }`; `DialogRow` ga `audioKey: string | null; fragen: Array<{ id: number; frageDe: string; frageUz: string; richtig: string; falsch: string[] }>`; map da `audioKey: d.audioKey, fragen: d.fragen` (Task 5 dagi bo'sh qiymatni almashtiring).

- [ ] **Step 5: Testlar, typecheck, commit**

Run: `npx jest src/daf/media --silent 2>&1 | tail -4 && npm run typecheck`
`frage-format-parity.spec.ts` endi YIQILADI (mijoz `media-fragen-types.ts` da `HOEREN_WAHL` yo'q) — bu kutilgan, Task 10 yopadi. Commitni shu yiqilish bilan qilmang: Task 10 ni tugatib, ikkalasini bitta commit qiling, YOKI parity testini vaqtincha o'tkazib yuborish uchun hech narsa qilmang — Task 10 shu shoxda darhol keladi. **Tanlov: Task 9 va 10 bitta commit.** Shu bosqichda faqat `git add` qiling:

```bash
git add src/daf/media/daf-media-fragen.service.ts src/daf/media/daf-media-inhalt.service.ts src/daf/media/daf-media-inhalt.service.spec.ts
```

---

### Task 10: Mijoz — pleyer, savol ekrani, transkript, `/media`

**Files:**
- Modify: `client/src/components/student-portal/lernen/types.ts`
- Modify: `client/src/components/student-portal/lernen/uebung/koersatma.ts`, `koersatma.test.ts`
- Create: `client/src/components/student-portal/lernen/uebung/pleyer-vaqt.ts`, `pleyer-vaqt.test.ts`
- Create: `client/src/components/student-portal/lernen/uebung/suhbat-pleyer.tsx`
- Create: `client/src/components/student-portal/lernen/uebung/transkript-blok.tsx`
- Modify: `client/src/components/student-portal/lernen/uebung/seans-ekrani.tsx` (~780–795 audio/prompt; ~875–895 natija paneli)
- Modify: `client/src/components/media/media-fragen-types.ts`, `media-fragen-utils.ts`, `media-fragen-utils.test.ts`, `media-fragen-panel.tsx`
- Modify: `client/src/components/media/media-inhalt-types.ts`, `media-inhalt-utils.test.ts` (`boshInhalt`), `media-inhalt-panel.tsx`

**Interfaces:**
- Consumes: server `PublicFrage` (o'zgarmagan), `PruefErgebnis.transkript`, `SectionInhalt.dialoge`, `VorschauFrage.itemType` ga `"HOERFRAGE"`.
- Produces: `formatVaqt(sekunden: number): string`; `<SuhbatPleyer url={string} />`; `<TranskriptBlok zeilen={...} />`.

- [ ] **Step 1: Yiqiladigan mijoz testlari**

`pleyer-vaqt.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { formatVaqt } from "./pleyer-vaqt";

describe("formatVaqt", () => {
  it("soniyani m:ss ko'rinishida beradi", () => {
    expect(formatVaqt(0)).toBe("0:00");
    expect(formatVaqt(5)).toBe("0:05");
    expect(formatVaqt(65)).toBe("1:05");
    expect(formatVaqt(600)).toBe("10:00");
  });

  it("kasrni pastga yaxlitlaydi", () => {
    expect(formatVaqt(41.9)).toBe("0:41");
  });

  it("NaN va Infinity (metadata hali yuklanmagan) — 0:00", () => {
    // `<audio>.duration` metadata kelguncha NaN; pleyer «NaN:NaN» chizmasin.
    expect(formatVaqt(Number.NaN)).toBe("0:00");
    expect(formatVaqt(Number.POSITIVE_INFINITY)).toBe("0:00");
  });
});
```

`koersatma.test.ts` da `HAMMASI` ro'yxatiga `"AUDIO_WORT", "WORT_TIPPEN", "HOEREN_WAHL"` (12 ta emas, 13 ta format — test nomlarini «har o'nta» dan «har bir» ga o'zgartiring) va:

```ts
  it("HOEREN_WAHL — TANLASH", () => {
    expect(harakat("HOEREN_WAHL")).toBe("TANLASH");
  });
```

`media-fragen-utils.test.ts` ga `expect(vorschauShakli("HOEREN_WAHL")).toBe("HOEREN");`.

- [ ] **Step 2: Yiqilishini tasdiqlang**

Run (client/): `npx vitest run src/components/student-portal/lernen/uebung src/components/media 2>&1 | tail -8`
Expected: `pleyer-vaqt` moduli yo'q; `koersatma`/`media-fragen-utils` da tip xatosi yoki yiqilish.

- [ ] **Step 3: Tiplar**

`lernen/types.ts`: `FrageFormat` ga `| "HOEREN_WAHL"`; `MaterialTyp` ga `| "HOERFRAGE"`; `PruefErgebnis`:

```ts
export interface PruefErgebnis {
  isCorrect: boolean;
  richtig: string;
  /**
   * FAQAT `HOEREN_WAHL` — suhbat matni javobdan KEYIN keladi. Savol bilan
   * birga hech qachon kelmaydi: kelsa, eshitish mashqi o'qish mashqiga
   * aylanardi.
   */
  transkript?: Array<{ sprecher: string; de: string; uz: string }>;
}
```

`media/media-fragen-types.ts`: `FrageFormat` ga `| "HOEREN_WAHL"`; `VorschauFrage.itemType` ga `| "HOERFRAGE"`.

`media/media-inhalt-types.ts`:

```ts
/** Butun dialog — karnay va savollar shu yerda (server: `InhaltDialog`). */
export interface InhaltDialog {
  id: number;
  code: string;
  titelDe: string;
  titelUz: string;
  audioUrl: string | null;
  fragen: Array<{
    code: string;
    frageDe: string;
    frageUz: string;
    richtig: string;
    falsch: string[];
  }>;
}
```

`SectionInhalt` ga `dialoge: InhaltDialog[];`. `media-inhalt-utils.test.ts` `boshInhalt()` ga `dialoge: [],`.

- [ ] **Step 4: `koersatma.ts`**

`MATN` ga `HOEREN_WAHL: "Suhbatni tinglang va savolga javob bering",`. `harakat` o'zgarmaydi (`TANLASH` — sukut).

- [ ] **Step 5: `pleyer-vaqt.ts`**

```ts
/**
 * Soniya → `m:ss`. `<audio>.duration` metadata kelguncha `NaN`, oqim
 * uchun `Infinity` — ikkalasi ham «0:00»: pleyer hech qachon
 * «NaN:NaN» chizmasin.
 */
export function formatVaqt(sekunden: number): string {
  if (!Number.isFinite(sekunden) || sekunden < 0) return "0:00";
  const s = Math.floor(sekunden);
  const m = Math.floor(s / 60);
  return `${m}:${String(s % 60).padStart(2, "0")}`;
}
```

- [ ] **Step 6: `suhbat-pleyer.tsx`**

```tsx
"use client";

import * as React from "react";
import { Pause, Play, ArrowClockwise } from "@phosphor-icons/react";
import { cn } from "@/lib/utils";
import { formatVaqt } from "./pleyer-vaqt";

/** O'quvchi tanlaydigan ikki tezlik — dizayn Q3: sekin variant faylsiz. */
const TEZLIKLAR = [1, 0.8] as const;

/**
 * Suhbat pleyeri — `HOEREN_WAHL` uchun.
 *
 * `OvozTugmasi` bir soniyalik so'z uchun («TO'LIQ PLEYER EMAS»); 40
 * soniyalik suhbatga pauza, vaqt chizig'i va tezlik kerak. Cheksiz
 * qayta eshitish; avtomatik boshlanadi (o'quvchi seansni tugma bosib
 * ochgan — brauzer ruxsat beradi; bermasa tugma qoladi, `catch`).
 *
 * `key={url}` — `OvozTugmasi`dagi bilan bir xil sabab: savol almashganda
 * eski `<audio>` tuguni butunlay olib tashlanadi, eski `error`/`play`
 * hodisalari yangi savolga tegmaydi.
 */
export function SuhbatPleyer({ url }: { url: string }) {
  const audioRef = React.useRef<HTMLAudioElement | null>(null);
  const [oynayapti, setOynayapti] = React.useState(false);
  const [vaqt, setVaqt] = React.useState(0);
  const [davomiylik, setDavomiylik] = React.useState(0);
  const [tezlik, setTezlik] = React.useState<(typeof TEZLIKLAR)[number]>(1);
  const [xato, setXato] = React.useState(false);

  const qoy = React.useCallback(() => {
    const a = audioRef.current;
    if (!a) return;
    setXato(false);
    void a.play().catch(() => setXato(true));
  }, []);

  React.useEffect(() => {
    qoy();
  }, [url, qoy]);

  React.useEffect(() => {
    const a = audioRef.current;
    if (a) a.playbackRate = tezlik;
  }, [tezlik, url]);

  const togla = () => {
    const a = audioRef.current;
    if (!a) return;
    if (a.paused) qoy();
    else a.pause();
  };

  const sur = (e: React.ChangeEvent<HTMLInputElement>) => {
    const a = audioRef.current;
    if (!a) return;
    a.currentTime = Number(e.target.value);
    setVaqt(a.currentTime);
  };

  return (
    <div className="space-y-3 rounded-2xl border border-line bg-surface p-4">
      <audio
        key={url}
        ref={audioRef}
        src={url}
        preload="auto"
        onPlay={() => setOynayapti(true)}
        onPause={() => setOynayapti(false)}
        onEnded={() => setOynayapti(false)}
        onTimeUpdate={(e) => setVaqt(e.currentTarget.currentTime)}
        onLoadedMetadata={(e) => {
          setDavomiylik(e.currentTarget.duration);
          e.currentTarget.playbackRate = tezlik;
        }}
        onError={() => setXato(true)}
      />
      <div className="flex items-center gap-3">
        <button
          type="button"
          onClick={togla}
          aria-label={xato ? "Ovozni qayta yuklash" : oynayapti ? "Pauza" : "Eshitish"}
          className="flex size-14 shrink-0 items-center justify-center rounded-full bg-primary text-primary-foreground transition-transform active:scale-95 motion-reduce:transition-none"
        >
          {xato ? (
            <ArrowClockwise size={26} weight="bold" />
          ) : oynayapti ? (
            <Pause size={26} weight="fill" />
          ) : (
            <Play size={26} weight="fill" />
          )}
        </button>
        <div className="flex min-w-0 flex-1 flex-col gap-1">
          <input
            type="range"
            min={0}
            max={davomiylik || 0}
            step={0.1}
            value={Math.min(vaqt, davomiylik || 0)}
            onChange={sur}
            aria-label="Suhbatning joyi"
            className="w-full accent-primary"
          />
          <div className="flex items-center justify-between text-xs tabular-nums text-ink-500">
            <span>{formatVaqt(vaqt)}</span>
            <span>{formatVaqt(davomiylik)}</span>
          </div>
        </div>
      </div>
      <div className="flex items-center gap-2">
        {TEZLIKLAR.map((t) => (
          <button
            key={t}
            type="button"
            onClick={() => setTezlik(t)}
            aria-pressed={tezlik === t}
            className={cn(
              "rounded-full px-3 py-1 text-sm font-semibold transition-colors",
              tezlik === t ? "bg-coral-500/10 text-coral-500" : "bg-tint text-ink-600",
            )}
          >
            {t === 1 ? "1×" : "🐢 0.8×"}
          </button>
        ))}
      </div>
      {xato ? (
        <p className="text-sm text-muted-foreground">Ovoz yuklanmadi — qayta urinib ko&apos;ring</p>
      ) : null}
    </div>
  );
}
```

- [ ] **Step 7: `transkript-blok.tsx`**

```tsx
"use client";

/**
 * Javobdan KEYIN ochiladigan suhbat matni — o'quvchi nimani
 * eshitmaganini ko'radi. Bu mashqning asosiy o'rganish lahzasi; savol
 * paytida ko'rsatilsa, eshitish mashqi o'qish mashqiga aylanardi.
 */
export function TranskriptBlok({
  zeilen,
}: {
  zeilen: Array<{ sprecher: string; de: string; uz: string }>;
}) {
  return (
    <div className="space-y-2 rounded-2xl border border-line bg-surface p-4">
      <p className="text-xs font-semibold uppercase tracking-wide text-ink-400">Suhbat matni</p>
      {zeilen.map((z, i) => (
        <div key={i} className="text-base leading-relaxed">
          <span className="font-semibold text-ink-500">{z.sprecher}: </span>
          <span className="text-ink-900">{z.de}</span>
          <span className="block text-sm text-ink-500">{z.uz}</span>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 8: `seans-ekrani.tsx`**

Importlar: `import { SuhbatPleyer } from "./suhbat-pleyer"; import { TranskriptBlok } from "./transkript-blok";`.

Audio bloki (`{frage.audioUrl ? (<OvozTugmasi url={frage.audioUrl} autoPlay />) : null}`) o'rniga:

```tsx
        {frage.format === "HOEREN_WAHL" && frage.audioUrl ? (
          // Suhbat — to'liq pleyer (pauza, vaqt, 0.8×). Savol paytida
          // MATN YO'Q; u `natija.transkript` bilan javobdan keyin keladi.
          <SuhbatPleyer url={frage.audioUrl} />
        ) : frage.audioUrl ? (
          <OvozTugmasi url={frage.audioUrl} autoPlay />
        ) : null}
```

Javob maydonidan KEYIN (`</main>` dan oldin, `rejim` shoxlaridan keyin):

```tsx
        {natija?.transkript ? <TranskriptBlok zeilen={natija.transkript} /> : null}
```

- [ ] **Step 9: `/media` mijozi**

`media-fragen-utils.ts`: `VorschauShakli` ga `| "HOEREN"`; `VORSCHAU_SHAKLI` ga `HOEREN_WAHL: "HOEREN",` (izoh: audio + savol matni birga).

`media-fragen-panel.tsx`: `FORMAT_NOMLARI` ga `HOEREN_WAHL: "Suhbatni eshitib savolga javob berish",`; `shakl === "DIALOG"` blokidan keyin:

```tsx
      {shakl === "HOEREN" && f.audioUrl && (
        <div className="space-y-1.5">
          {f.titel && (
            <div className="text-xs font-medium text-muted-foreground">{f.titel}</div>
          )}
          <OvozTugmasi url={f.audioUrl} autoPlay={false} compact />
          <div className="text-sm">{f.prompt}</div>
        </div>
      )}
```

`media-inhalt-panel.tsx` `DialogBlock` ni dialog bo'yicha guruhlaydigan qiling:

```tsx
function DialogBlock({
  dialoge,
  rows,
}: {
  dialoge: InhaltDialog[];
  rows: InhaltDialogZeile[];
}) {
  if (dialoge.length === 0 && rows.length === 0) return null;
  return (
    <div className="space-y-4">
      <h3 className="text-sm font-semibold">Dialoglar</h3>
      {dialoge.map((d) => (
        <div key={d.id} className="space-y-2 rounded-md border p-3">
          <div className="flex items-center justify-between gap-3">
            <div>
              <div className="font-medium">{d.titelDe}</div>
              <div className="text-xs text-muted-foreground">{d.titelUz} · {d.code}</div>
            </div>
            {/* Butun suhbatning ovozi — CEO namunani shu yerda eshitadi. */}
            <OvozHujayrasi url={d.audioUrl} />
          </div>
          <Table>
            <TableHeader>
              <TableRow>
                <TableHead className="w-28">So&apos;zlovchi</TableHead>
                <TableHead>Nemischa</TableHead>
                <TableHead>Tarjima</TableHead>
              </TableRow>
            </TableHeader>
            <TableBody>
              {rows
                .filter((r) => r.dialogId === d.id)
                .map((r) => (
                  <TableRow key={r.id}>
                    <TableCell className="text-muted-foreground">{r.sprecher}</TableCell>
                    <TableCell className="font-medium">{r.de}</TableCell>
                    <TableCell className="text-muted-foreground">{r.uz}</TableCell>
                  </TableRow>
                ))}
            </TableBody>
          </Table>
          {d.fragen.length > 0 && (
            <ul className="space-y-1 text-sm">
              {d.fragen.map((f) => (
                <li key={f.code}>
                  <span className="font-medium">{f.frageDe}</span>{" "}
                  <span className="text-muted-foreground">({f.frageUz})</span>
                  {" → "}
                  <span className="text-emerald-700 dark:text-emerald-400">{f.richtig}</span>
                  <span className="text-muted-foreground"> · {f.falsch.join(" · ")}</span>
                </li>
              ))}
            </ul>
          )}
        </div>
      ))}
    </div>
  );
}
```

Chaqiruv: `<DialogBlock dialoge={data.dialoge} rows={data.dialogZeilen} />`; importga `InhaltDialog`. Eski «Ovoz» ustuni (satr darajasi) olib tashlanadi — satr audiosi bu bosqichda yo'q (dizayn §9).

- [ ] **Step 10: Mijoz testlari, tip, lint**

Run (client/): `npm test -- --run 2>&1 | tail -5 && npx tsc --noEmit && npx eslint src/components/student-portal/lernen src/components/media 2>&1 | tail -1`
Expected: PASS, toza, 0 error.

Run (server/): `npx jest src/daf/media/frage-format-parity.spec.ts`
Expected: PASS — endi ikkala tomon `HOEREN_WAHL` ni biladi.

- [ ] **Step 11: Commit (Task 9 bilan birga)**

```bash
git add client/src/components/student-portal/lernen client/src/components/media
git commit -m "Eshitish mashqi ekrani: suhbat pleyeri (0.8×), javobdan keyingi transkript, /media ko'rigi

Suhbat matni savol paytida yo'q — u faqat javob bilan keladi. /media da
har dialog o'z karnayi va savollari bilan; CEO ovoz namunasini shu yerda
eshitadi.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 11: Ovoz skripti va BITTA dialog namunasi (darvoza)

**Files:**
- Create: `server/content/daf/a1/stimmen.json`
- Modify: `server/src/daf/media/fal-client.ts`, `fal-client.spec.ts`
- Create: `server/scripts/daf-gen-dialog-audio.ts`, `server/scripts/daf-gen-dialog-audio.spec.ts`
- Modify: `server/package.json`
- Create (skript yozadi): `server/content/daf/a1/dialog-audio.json`

**Interfaces:**
- Consumes: `dialogTextHash`, `DialogAudioManifest` (Task 2), `neuerAudioSchluessel`, `R2Uploader`, `Dialog`.
- Produces: `FalClient.dialog(inputs: Array<{ voice: string; text: string }>): Promise<string>`; skript `npm run daf:gen-dialog-audio -- --dialog u02-d2` / `-- --unit 2`.

- [ ] **Step 1: `stimmen.json`**

Ruxsat etilgan ovozlar (fal OpenAPI, 2026-09-11): Aria, Roger, Sarah, Laura, Charlie, George, Callum, River, Liam, Charlotte, Alice, Matilda, Will, Jessica, Eric, Chris, Brian, Daniel, Lily, Bill. Har obrazga bittadan, jinsi va yoshiga qarab, kurs davomida o'zgarmaydi:

```json
{
  "Jonas": "Liam",
  "Mia": "Lily",
  "Walter": "George",
  "Claudia": "Sarah",
  "Markus": "Chris",
  "Doniyor": "Eric",
  "Anna": "Aria",
  "Sabine": "Matilda",
  "Peter": "Daniel",
  "Helga": "Charlotte",
  "Timur": "Will",
  "Nodira": "Jessica"
}
```

- [ ] **Step 2: Yiqiladigan `FalClient.dialog` testi**

`fal-client.spec.ts` ga:

```ts
describe('FalClient.dialog', () => {
  it('butun suhbatni bitta so`rovda yuboradi, de tili va stability bilan', async () => {
    const calls: Array<{ url: string; body: any }> = [];
    const fetchFn = (async (url: string, init: any) => {
      calls.push({ url, body: JSON.parse(init.body) });
      return {
        ok: true, status: 200,
        json: async () => ({ audio: { url: 'https://x/d.mp3' }, seed: 1 }),
        text: async () => '',
      };
    }) as unknown as typeof fetch;
    const c = new FalClient('k', fetchFn);
    const url = await c.dialog([
      { voice: 'Aria', text: 'Ist das deine Schwester?' },
      { voice: 'Liam', text: 'Nein.' },
    ]);
    expect(url).toBe('https://x/d.mp3');
    expect(calls[0].url).toBe(
      'https://fal.run/fal-ai/elevenlabs/text-to-dialogue/eleven-v3',
    );
    expect(calls[0].body).toEqual({
      inputs: [
        { voice: 'Aria', text: 'Ist das deine Schwester?' },
        { voice: 'Liam', text: 'Nein.' },
      ],
      language_code: 'de',
      stability: 0.5,
    });
  });

  it('audio qaytmasa yiqiladi', async () => {
    const c = new FalClient('k', fetchStub({ seed: 1 }));
    await expect(c.dialog([{ voice: 'Aria', text: 'x' }])).rejects.toThrow(/suhbat/i);
  });
});
```

- [ ] **Step 3: Yiqilishini tasdiqlang; `dialog()` ni yozing**

Run: `npx jest src/daf/media/fal-client.spec.ts 2>&1 | grep -E "●.*›|Tests:"`

`fal-client.ts` ga konstanta `const DIALOG_MODEL = 'fal-ai/elevenlabs/text-to-dialogue/eleven-v3';` va metod:

```ts
  /**
   * Butun suhbat BITTA so'rovda — gapiruvchilar bir-biriga javob
   * beradi, ohang tabiiy chiqadi (ovoz tizimi qarori, 2026-09-02).
   * `language_code: 'de'` qat'iy (ingliz fonetikasiga tushmasin),
   * `stability: 0.5` sinovdan keyin O'ZGARMAYDI — aks holda 12 dialog
   * bir-biridan farq qilib eshitiladi. Tezlik parametri modelda YO'Q —
   * sekin variant mijoz pleyerida (0.8×).
   */
  async dialog(
    inputs: Array<{ voice: string; text: string }>,
  ): Promise<string> {
    const out = await this.run(DIALOG_MODEL, {
      inputs,
      language_code: 'de',
      stability: 0.5,
    });
    const url = out?.audio?.url;
    if (typeof url !== 'string') throw new Error('fal.ai suhbat qaytarmadi');
    return url;
  }
```

- [ ] **Step 4: Skriptning sof qismlari uchun yiqiladigan test**

`scripts/daf-gen-dialog-audio.spec.ts`:

```ts
import {
  dialogInputs,
  gesamtZeichenDialoge,
  pruefeDialogBudget,
  zuGenerierenDialoge,
  BELGI_CHEGARASI_DIALOG,
} from './daf-gen-dialog-audio';
import { dialogTextHash } from '../src/daf/inhalt/dialog-audio';
import type { Dialog } from '../src/daf/inhalt/unit-inhalt.types';

const stimmen = { Anna: 'Aria', Jonas: 'Liam' };

const d = (id: string): Dialog => ({
  id,
  section: 'u02-s1',
  titelDe: 'x',
  titelUz: 'x',
  zeilen: [
    { sprecher: 'Anna', de: 'Ist das deine Schwester?', uz: 'x' },
    { sprecher: 'Jonas', de: 'Nein.', uz: 'x', tts: 'Nain.' },
  ],
});

describe('dialogInputs', () => {
  it('har satrga obrazning ovozini biriktiradi, matn tts ?? de', () => {
    expect(dialogInputs(d('u02-d2'), stimmen)).toEqual([
      { voice: 'Aria', text: 'Ist das deine Schwester?' },
      { voice: 'Liam', text: 'Nain.' },
    ]);
  });

  it('ro`yxatda yo`q gapiruvchida TO`XTAYDI — jimgina standart ovoz yo`q', () => {
    expect(() => dialogInputs(d('u02-d2'), { Anna: 'Aria' })).toThrow(
      /Jonas.*stimmen\.json/,
    );
  });
});

describe('zuGenerierenDialoge', () => {
  it('manifestda xeshi mos dialogni o`tkazib yuboradi — idempotent', () => {
    const a = d('u02-d1');
    const manifest = {
      'u02-d1': { key: 'daf/audio/a.mp3', textHash: dialogTextHash(a.zeilen) },
    };
    expect(zuGenerierenDialoge([a, d('u02-d2')], manifest).map((x) => x.id)).toEqual(['u02-d2']);
  });

  it('xeshi eskirgan dialogni QAYTA yasaydi', () => {
    const a = d('u02-d1');
    const manifest = { 'u02-d1': { key: 'daf/audio/a.mp3', textHash: '0000' } };
    expect(zuGenerierenDialoge([a], manifest).map((x) => x.id)).toEqual(['u02-d1']);
  });
});

describe('budjet', () => {
  it('belgi sonini tts ?? de bo`yicha sanaydi', () => {
    expect(gesamtZeichenDialoge([d('x')])).toBe(
      'Ist das deine Schwester?'.length + 'Nain.'.length,
    );
  });

  it('chegaradan oshsa chaqiruvdan OLDIN to`xtaydi', () => {
    expect(() => pruefeDialogBudget(BELGI_CHEGARASI_DIALOG + 1)).toThrow(/TO'XTATILDI/);
    expect(() => pruefeDialogBudget(BELGI_CHEGARASI_DIALOG)).not.toThrow();
  });
});
```

- [ ] **Step 5: Skriptni yozing**

`server/scripts/daf-gen-dialog-audio.ts`:

```ts
/**
 * Dialoglarga ovoz yasaydi (`text-to-dialogue/eleven-v3`) va R2 ga yuklaydi.
 *
 *   npm run daf:gen-dialog-audio -- --dialog u02-d2     — bitta (namuna darvozasi)
 *   npm run daf:gen-dialog-audio -- --unit 2            — unitning hammasi
 *   npm run daf:gen-dialog-audio -- --unit 1 --unit 2   — bir nechta unit
 *
 * PULLIK: $0.10 / 1 000 belgi (2026-09-11). Manifestda xeshi mos dialog
 * QAYTA yasalmaydi — qayta yuritish pul sarflamaydi. Xeshi eskirgan
 * (matni tahrirlangan) dialog qayta yasaladi va eski kalit almashadi.
 *
 * Ro'yxatda (`stimmen.json`) yo'q gapiruvchi — TO'XTASH, standart ovoz
 * yo'q: ovoz obrazga biriktirilgan va kurs davomida o'zgarmaydi.
 *
 * `main()` faqat fayl to'g'ridan-to'g'ri ishga tushirilganda yuguradi
 * (`require.main === module`) — testlar import qilganda tarmoq yo'q.
 */
import 'dotenv/config';
import { existsSync, readFileSync, writeFileSync } from 'fs';
import { join } from 'path';
import { S3Client } from '@aws-sdk/client-s3';
import { R2Uploader } from '../src/daf-content/media/r2-uploader';
import type { AssetRef } from '../src/daf-content/dataset.types';
import { FalClient } from '../src/daf/media/fal-client';
import { neuerAudioSchluessel } from '../src/daf/media/audio-keys';
import {
  dialogTextHash,
  type DialogAudioManifest,
} from '../src/daf/inhalt/dialog-audio';
import type { Dialog, DialogeFile } from '../src/daf/inhalt/unit-inhalt.types';

const A1 = join(__dirname, '..', 'content', 'daf', 'a1');
const MANIFEST_PATH = join(A1, 'dialog-audio.json');
const STIMMEN_PATH = join(A1, 'stimmen.json');

/**
 * Bir yuritishdagi belgi chegarasi. 12 dialog = 1 592 belgi; chegara
 * 2 500 — tasodifan butun boshqa unit qo'shilib qolsa to'xtaydi.
 */
export const BELGI_CHEGARASI_DIALOG = 2500;

const REQUIRED_ENV = [
  'FAL_KEY',
  'R2_ENDPOINT',
  'R2_ACCESS_KEY_ID',
  'R2_SECRET_ACCESS_KEY',
  'R2_BUCKET_NAME',
];

export type Stimmen = Record<string, string>;

/** Har satr → `{ voice, text }`; matn `tts ?? de`. Noma'lum gapiruvchi — xato. */
export function dialogInputs(
  dialog: Dialog,
  stimmen: Stimmen,
): Array<{ voice: string; text: string }> {
  return dialog.zeilen.map((z) => {
    const voice = stimmen[z.sprecher];
    if (!voice) {
      throw new Error(
        `${dialog.id}: «${z.sprecher}» uchun ovoz yo'q — stimmen.json ga qo'shing. ` +
          "Standart ovoz ATAYLAB yo'q: obraz ovozi kurs davomida o'zgarmaydi.",
      );
    }
    return { voice, text: z.tts ?? z.de };
  });
}

/** Manifestda xeshi mos dialoglar o'tkazib yuboriladi — idempotentlik. */
export function zuGenerierenDialoge(
  dialoge: Dialog[],
  manifest: DialogAudioManifest,
): Dialog[] {
  return dialoge.filter(
    (d) => manifest[d.id]?.textHash !== dialogTextHash(d.zeilen),
  );
}

export function gesamtZeichenDialoge(dialoge: Dialog[]): number {
  return dialoge.reduce(
    (sum, d) => sum + d.zeilen.reduce((s, z) => s + (z.tts ?? z.de).length, 0),
    0,
  );
}

export function pruefeDialogBudget(zeichen: number): void {
  if (zeichen > BELGI_CHEGARASI_DIALOG) {
    throw new Error(
      `Narx chegarasi oshib ketdi: ${zeichen} belgi (chegara ${BELGI_CHEGARASI_DIALOG}). ` +
        "Chaqiruv TO'XTATILDI, hech narsa yuborilmadi.",
    );
  }
}

function argValues(flag: string): string[] {
  const out: string[] = [];
  process.argv.forEach((a, i) => {
    if (a === flag && process.argv[i + 1]) out.push(process.argv[i + 1]);
  });
  return out;
}

function ladeDialoge(): Dialog[] {
  const units = argValues('--unit').map(
    (n) => `u${String(Number(n)).padStart(2, '0')}`,
  );
  const einzeln = argValues('--dialog');
  if (units.length === 0 && einzeln.length === 0) {
    throw new Error('Kerak: --unit <raqam> yoki --dialog <kod>');
  }
  for (const code of einzeln) {
    const unit = code.slice(0, 3);
    if (!units.includes(unit)) units.push(unit);
  }
  const alle = units.flatMap(
    (u) =>
      (JSON.parse(readFileSync(join(A1, u, 'dialoge.json'), 'utf8')) as DialogeFile)
        .dialoge,
  );
  return einzeln.length > 0 ? alle.filter((d) => einzeln.includes(d.id)) : alle;
}

async function main(): Promise<void> {
  const stimmen = JSON.parse(readFileSync(STIMMEN_PATH, 'utf8')) as Stimmen;
  const manifest: DialogAudioManifest = existsSync(MANIFEST_PATH)
    ? (JSON.parse(readFileSync(MANIFEST_PATH, 'utf8')) as DialogAudioManifest)
    : {};

  const qoldi = zuGenerierenDialoge(ladeDialoge(), manifest);
  console.log(`${qoldi.length} ta dialogga ovoz kerak.`);
  if (qoldi.length === 0) {
    console.log("Qilinadigan ish yo'q.");
    return;
  }
  // Gapiruvchilar TARMOQDAN OLDIN tekshiriladi — yarim yasab to'xtamasin.
  const inputsById = new Map(qoldi.map((d) => [d.id, dialogInputs(d, stimmen)]));

  const zeichen = gesamtZeichenDialoge(qoldi);
  console.log(`Jami belgi: ${zeichen} (≈ $${((zeichen / 1000) * 0.1).toFixed(3)}).`);
  pruefeDialogBudget(zeichen);

  const missingEnv = REQUIRED_ENV.filter((k) => !process.env[k]);
  if (missingEnv.length > 0) {
    throw new Error(`Sozlanmagan muhit o'zgaruvchisi: ${missingEnv.join(', ')}`);
  }

  const fal = new FalClient(process.env.FAL_KEY!);
  const s3 = new S3Client({
    region: 'auto',
    endpoint: process.env.R2_ENDPOINT!,
    credentials: {
      accessKeyId: process.env.R2_ACCESS_KEY_ID!,
      secretAccessKey: process.env.R2_SECRET_ACCESS_KEY!,
    },
  });
  const uploader = new R2Uploader(s3, process.env.R2_BUCKET_NAME!);

  const assets: AssetRef[] = [];
  const keyById = new Map<string, string>();
  for (const d of qoldi) {
    const sourceUrl = await fal.dialog(inputsById.get(d.id)!);
    const key = neuerAudioSchluessel();
    keyById.set(d.id, key);
    assets.push({
      sourceUrl,
      key,
      kind: 'AUDIO',
      license: 'Generated',
      attribution: 'DaF Sprachzentrum — fal.ai ElevenLabs text-to-dialogue v3',
      title: d.titelDe,
    });
    console.log(`  ${d.id}: ${d.titelDe} → yasaldi`);
  }

  const r = await uploader.uploadMissing(assets);
  console.log(`R2: yuklandi ${r.uploaded}, o'tkazildi ${r.skipped}, yiqildi ${r.failed.length}`);
  const failed = new Set(r.failed.map((f) => f.key));

  // FAQAT muvaffaqiyatli yuklangan kalit manifestga — aks holda o'quvchi
  // yangramaydigan pleyerni ko'rardi.
  for (const d of qoldi) {
    const key = keyById.get(d.id)!;
    if (failed.has(key)) {
      console.error(`  DIQQAT: ${d.id} R2 ga yuklanmadi, manifestga YOZILMADI`);
      process.exitCode = 1;
      continue;
    }
    manifest[d.id] = { key, textHash: dialogTextHash(d.zeilen) };
  }
  writeFileSync(MANIFEST_PATH, JSON.stringify(manifest, null, 2) + '\n');
  console.log(`Manifest yangilandi: ${MANIFEST_PATH}`);
}

if (require.main === module) {
  void main().catch((e: unknown) => {
    console.error((e as Error).message);
    process.exit(1);
  });
}
```

`package.json` ga `"daf:gen-dialog-audio": "ts-node scripts/daf-gen-dialog-audio.ts",`.

- [ ] **Step 6: Testlar, typecheck, lint, commit**

Run: `npx jest src/daf/media/fal-client.spec.ts scripts/daf-gen-dialog-audio.spec.ts --silent 2>&1 | tail -4 && npm run typecheck && npx tsc --noEmit -p tsconfig.json 2>&1 | grep "daf-gen-dialog-audio" ; npx prettier --write scripts/daf-gen-dialog-audio.ts scripts/daf-gen-dialog-audio.spec.ts src/daf/media/fal-client.ts src/daf/media/fal-client.spec.ts`
Expected: PASS; typecheck toza; `grep` hech narsa chiqarmaydi (skriptda tip xatosi yo'q).

```bash
git add content/daf/a1/stimmen.json src/daf/media/fal-client.ts src/daf/media/fal-client.spec.ts scripts/daf-gen-dialog-audio.ts scripts/daf-gen-dialog-audio.spec.ts package.json
git commit -m "Dialog audiosi skripti: text-to-dialogue, obraz ovozlari, idempotent manifest

Har obrazga bitta ElevenLabs ovozi (stimmen.json), ro'yxatda yo'q
gapiruvchida to'xtaydi. Manifestda xeshi mos dialog qayta yasalmaydi.
Hali hech narsa chaqirilmadi — namuna CEO ruxsati bilan.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

- [ ] **Step 7: 🔒 DARVOZA — narx va ruxsat**

`mcp__fal-ai__get_pricing` ni `fal-ai/elevenlabs/text-to-dialogue/eleven-v3` uchun chaqiring. `u02-d2` belgi soni: `node -e "const d=require('./content/daf/a1/u02/dialoge.json').dialoge.find(x=>x.id==='u02-d2');console.log(d.zeilen.reduce((s,z)=>s+(z.tts??z.de).length,0))"`. CEO ga: «u02-d2, N belgi, ≈ $0.0X — yasaymi?». **Ruxsatsiz davom etmang.**

- [ ] **Step 8: Namunani yasang (ruxsatdan keyin)**

`FAL_KEY` `.env` da YO'Q — fal MCP tokeni `~/.claude.json` da (`mcpServers.fal-ai.headers.Authorization`, `Bearer …`). Jarayon ichida o'qib uzating, hech qayerga chop etmang:

```bash
cd /Users/a1111/Desktop/daf-erp-system/.worktrees/a1-kichik-qarzlar/server
export FAL_KEY="$(node -e 'const c=require(require("os").homedir()+"/.claude.json");process.stdout.write(c.mcpServers["fal-ai"].headers.Authorization.replace(/^Bearer\s+/,""))')"
for k in R2_ENDPOINT R2_ACCESS_KEY_ID R2_SECRET_ACCESS_KEY R2_BUCKET_NAME; do export "$k=$(grep -m1 "^$k=" /Users/a1111/Desktop/daf-erp-system/server/.env | cut -d= -f2- | tr -d '"'"'"'\r')"; done
npm run daf:gen-dialog-audio -- --dialog u02-d2
```

Expected: `1 ta dialogga ovoz kerak.` → `Jami belgi: …` → `u02-d2: Meine Schwester → yasaldi` → `R2: yuklandi 1` → manifestda `u02-d2`.

- [ ] **Step 9: Lokal seed va `/media` da eshitish**

Run: `export DATABASE_URL=...; npx ts-node scripts/daf-inhalt-seed.ts --unit 2 | tail -3`, keyin lokal serverni yoqib `/media` → u02-s1 bo'limi → «Meine Schwester» karnayi. **CEO eshitadi**: ovozlar, tezlik, «Nodira» talaffuzi. Xato talaffuz bo'lsa — o'sha satrga `tts` yozib (`"tts": "Nodiera"` kabi), manifestdan `u02-d2` ni o'chirib, qayta yasang (xesh o'zgargani uchun skript baribir qayta yasaydi).

- [ ] **Step 10: Commit (manifest)**

```bash
git add content/daf/a1/dialog-audio.json content/daf/a1/u02/dialoge.json
git commit -m "u02-d2 dialog audiosi — namuna, CEO tasdiqladi

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 12: Qolgan 11 dialog, chiqarish

**Files:**
- Modify: `server/content/daf/a1/dialog-audio.json` (skript)
- Deploy: Railway (server), Vercel (client), prod seed.

- [ ] **Step 1: 🔒 DARVOZA — narx va ruxsat**

`get_pricing` qayta; qolgan 11 dialogning belgi soni: `node -e "const h=require('./content/daf/a1/dialog-audio.json');let n=0;for(const u of ['u01','u02'])for(const d of require('./content/daf/a1/'+u+'/dialoge.json').dialoge)if(!h[d.id])n+=d.zeilen.reduce((s,z)=>s+(z.tts??z.de).length,0);console.log(n)"`. CEO ga: «11 dialog, N belgi, ≈ $0.15 — yasaymi?». **Ruxsatsiz davom etmang.**

- [ ] **Step 2: Yasang**

Task 11 Step 8 dagi muhit bilan: `npm run daf:gen-dialog-audio -- --unit 1 --unit 2`
Expected: `11 ta dialogga ovoz kerak.` (u02-d2 o'tkazib yuborilgan), `R2: yuklandi 11`.

- [ ] **Step 3: Qo'riqchi, seed, test**

Run: `npx jest src/daf/inhalt --silent 2>&1 | tail -3; npm run daf:inhalt-check -- --unit 1 | tail -1; npm run daf:inhalt-check -- --unit 2 | tail -1; npx ts-node scripts/daf-inhalt-seed.ts --unit 1 | tail -2; npx ts-node scripts/daf-inhalt-seed.ts --unit 2 | tail -2`
Expected: toza; ikkala seed o'tadi. Lokal `/portal/lernen` da u01 yoki u02 yakuniy sinovini oching — eshitish savoli chiqadi, javobdan keyin transkript ochiladi.

- [ ] **Step 4: Commit va PR**

```bash
git add content/daf/a1/dialog-audio.json
git commit -m "1 va 2-unitning 12 dialogiga ovoz — eshitish mashqi to'liq

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
git push -u origin feat/a1-hoeren
gh pr create --base main --head feat/a1-hoeren --title "Eshitish mashqi (HOEREN_WAHL): 12 dialogga ovoz va 24 tushunish savoli" --body "$(cat <<'PR'
## Nima qilindi
- Yangi format `HOEREN_WAHL`: suhbatni eshitib 3 variantdan javob; suhbat matni faqat javobdan keyin.
- `DafHoerFrage` jadvali, `DafDialog.audioKey`; migratsiya `20260911120000_daf_hoer_frage`.
- 1 va 2-unitning 12 dialogiga ovoz (`text-to-dialogue/eleven-v3`, ≈ $0.16), 24 savol qo'lda.
- Qo'riqchi: savol qoidalari, `textHash` (matn o'zgarsa audio eskirgani ushlanadi).
- `DIALOG_LUECKE` endi suhbatning hamma satrini band qiladi — bir suhbat bir seansda ikki marta chiqmaydi.
- `itemType` ro'yxati kompilyator qo'riqlaydigan `Record` ga o'tdi.
- Mijoz: suhbat pleyeri (0.8×), transkript, `/media` da dialog karnayi va savollari.

## Deploy
- Railway (migratsiya `start:prod` da o'zi) → prodda `daf:inhalt-seed --unit 1` va `--unit 2` SHART.
- Vercel (client o'zgargan).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
PR
)"
```

- [ ] **Step 5: 🔒 DARVOZA — CEO deploy ruxsati**

CI yashil bo'lgach CEO ga ayting. Ruxsatsiz merge/deploy qilmang.

- [ ] **Step 6: Deploy (ruxsatdan keyin)**

```bash
# server (worktree'ning server/ ida .railway allaqachon bog'langan)
cd server && railway up --detach
# holat SUCCESS bo'lguncha kuting (railway status --json → serviceInstances[caring-courage].latestDeployment.status)
# client
cd ../client && vercel --prod --yes
# merge
gh pr merge --merge
# prod seed — deploydan KEYIN (migratsiya qo'llangan bo'lishi kerak)
cd ../server && railway run npm run daf:inhalt-seed -- --unit 1 && railway run npm run daf:inhalt-seed -- --unit 2
```

Expected: ikkala prod seed hisobotida `"hoerFragen": 12`; `railway run npx ts-node -e` bilan prod `DafDialog` da `audioKey` 12/12 ekanini tekshiring (o'qish, yozish emas).

- [ ] **Step 7: Tekshiruv va xotira**

Prod `/portal/lernen` → yakuniy sinov → eshitish savoli chiqadi (CEO telefonda). Xotira: `project_a1_kurs_poydevor.md` (Hören qamrovi o'zgardi), yangi `project_a1_hoeren.md` (ovoz tartibi, `stimmen.json`, `textHash` qoidasi, narx).

---

## Self-Review

**Spec coverage:** §3 ekran → Task 10; §4.1 fayl shakli → Task 2/4; §4.2 qoidalar 1–5 → Task 2; §5 model + 5.1 seed → Task 1/3; §6 dvigatel (itemType Record, hoer-fragen, belegteItems, cap, mediaUrl, moyillik, pruefen/transkript, /media) → Task 5/6/7/8/9; §7.1 stimmen → Task 11; §7.2 skript/stability/namuna → Task 11; §7.3 manifest/textHash → Task 2/11; §7.4 narx darvozalari → Task 11 Step 7, Task 12 Step 1; §8 tartib → Task 1–12; §10 xavflar — har biri tegishli taskda.

**Placeholder scan:** hech qanday TBD/TODO yo'q; har kod bosqichida kod bor.

**Type consistency:** `ItemType` (Task 5) — `frage.types.ts`, DTO, service, client `MaterialTyp`; `MaterialDialog.audioKey/fragen` (Task 5) — Task 6/8/9 da ishlatiladi; `PruefenErgebnis.transkript` (Task 8) — client `PruefErgebnis.transkript` (Task 10); `InhaltDialog` (Task 9) — client (Task 10); `dialogTextHash`/`DialogAudioManifest` (Task 2) — Task 3/11; `hoerenWahl(dialog, rnd, mediaUrl)` (Task 6) — Task 8/9; `capFuer` (Task 7) — seans.ts ichida.
