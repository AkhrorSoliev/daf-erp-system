Bold gradient hero tile used on Home / Resources. Large rounded title, optional pill subtitle, corner open-arrow, right-side art slot.

```jsx
<FeatureCard gradient="cool" title="Mening lug‘atim" subtitle="Hali so‘zlar yo‘q"
  art={<i className="ph-fill ph-cards" />} onClick={open} />
```

Gradients: warm | sun | teal | cool | grape — each gets a matching clay lip.
