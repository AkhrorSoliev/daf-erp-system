Ranked row for the leaderboard ("Peshqadamlar jadvali"). Top-3 ranks render in coral; `highlight` outlines the signed-in user's row.

```jsx
<LeaderboardRow rank={1} name="Aliyor Sarmanov" xp="10722" />
<LeaderboardRow rank={73764} name="jack" xp="202" highlight />
```
