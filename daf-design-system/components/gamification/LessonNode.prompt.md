Chunky rounded unit tile for the Duolingo-style lesson path.

```jsx
<LessonNode label="Unit 1.1" percent={0} state="active" onClick={openUnit} />
```

States: `locked` (grey, sunk, lock icon), `active` (white + coral glow ring), `done` (teal).
