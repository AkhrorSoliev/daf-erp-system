Selectable filter / choice chip. Outlined when idle, solid accent when selected.

```jsx
<Chip selected color="coral">Hammasi</Chip>
<Chip iconBefore={<i className="ph-bold ph-star" />}>Yangi</Chip>
```
