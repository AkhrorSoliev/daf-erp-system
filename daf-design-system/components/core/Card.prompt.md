Base rounded surface — the container under almost every Lumio element.

```jsx
<Card pad="lg">…</Card>
<Card clay tone="coral" radius="xl">Feature</Card>
```

`clay` switches from soft ambient shadow to the extruded bottom-lip look; `tone` colors that lip. `pad`: none|sm|md|lg. `radius`: md|lg|xl|2xl.
