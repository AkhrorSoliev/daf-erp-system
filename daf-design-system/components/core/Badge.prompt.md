Tiny rounded label/status pill.

```jsx
<Badge color="amber">[noun]</Badge>
<Badge color="success" soft={false}>Tugallandi</Badge>
```

Colors span the full palette plus `success`/`danger`/`neutral`. `soft` (default) = tinted; `soft={false}` = solid fill.
