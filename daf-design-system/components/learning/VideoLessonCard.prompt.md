Video lesson block — title, a "Video" and a "Mashq" (practice) progress row each with star/coin fractions + track bar, and a "Videoni ko‘rish" watch button.

```jsx
<VideoLessonCard title="1-video"
  video={{ stars:[0,1], coins:[0,1], pct:0 }}
  practice={{ stars:[0,10], coins:[0,10], pct:0 }}
  onWatch={play} />
```
